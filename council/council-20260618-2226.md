# Daily council — 2026-06-18 22:26

## The General's briefing

**SITREP** — 6 tickets run: 4 merged to DEV, 1 errored, 1 still running. Avg 1.33 passes/ticket but 4 of 6 hit max effort, all bleeding from the same wound: a11y ×6 and tests ×5, every bounce caught in the Inspector loop on the FE side. The Drillmaster's axe-clean + non-negative-coverage exit gate landed last cycle but has **zero tickets through it** — unproven. Three verification posts (Scout/QA, Provost/Security, Quartermaster/Deploy) sit advisory with their gates unbuilt; security and deploy gates have never once fired. The lone running ticket has no QA owner to receive it.

**ORDERS FOR TODAY**
- **Drillmaster** — land one Knowledge line in `frontend-engineer.md`: keyboard happy-path test (tab every control, operate it, assert focus visible/never-lost, zero console errors), with Scout's clause to assert against the assembled DEV build where a flow crosses a route change or live region. Single build-side add this cycle.
- **Field Engineer + Inspector** — run the next FE ticket under the gate, instrumented: a11y/coverage **blocking**, pre/post-gate tagged; Inspector cross-checks his auditors against those numbers using the keyboard test as the verification artifact. Report bounce numbers next council.
- **Provost (secret-scan) + Quartermaster (migration-file-exists)** — both ride as **blocking exit checks now**, not observation: leaked Supabase key and silent schema divergence are binary and irreversible — no rotation un-leaks them, no passes/ticket drag. Tenant-scope and env-parity ride **observation-only** this cycle to build evidence.
- **Quartermaster** — verify on this ticket whether DEV↔MAIN migration state already matches; if the six landed tickets have diverged it, that's a today problem.
- **Adjutant** — HOLD the corps, no new hire. Sequence post activation on data: Scout (QA) next cycle once the gate proves out, then the observation tags graduate to gates one per cycle.

**FOR THE COMMANDER** — None.

## The record

```
Tasks run: 6
Outcomes: merged→dev: 4, errored: 1, running: 1
Avg passes/ticket: 1.33 | retried: 1 | hit max effort: 4
Gate failures: 0 | decisions needed: 0
Recurring Inspector issue areas: a11y ×6, tests ×5, regression ×1, correctness ×1, quality ×1, performance ×1
```

## Round-table

### Adjutant

S-1 Adjutant, reporting from the roster.

I read the corps: Engineer, Inspector, and Drillmaster are in active post; Scout (QA), Provost (Security), and Quartermaster (DevOps) remain PLANNED per `officers/README.md`. The record's dominant recurring gap — a11y ×6, tests ×5 — already has owners: Vanguard (FE) inside the Engineer's squad, the Inspector's auditors as verifiers, and now the Drillmaster's hard exit-gate on `frontend-engineer.md` landed this cycle. That is a build-side training fix, not a missing officer — recruiting a dedicated a11y officer now would pad the unit before we've measured whether the new gate drops the bounces. Per our own Lessons log, we hold roster and re-examine after the gate proves out; we have not yet seen a single ticket run under it.

The one forming gap worth naming: the mission ends in "QA on DEV," yet Scout (QA) is still unbuilt — that verification step currently has no owner, and one ticket sits in `running`. I am not recruiting today, but Scout is the next post to stand up once the a11y gate's effect is confirmed.

**Recommendation for today: HOLD the corps — no new hire. Let the Drillmaster's a11y/test gate run one full cycle; I'll re-assess Scout (QA) activation next council with passes/ticket data in hand.**

### Field Engineer

S-3 Field Engineer, reporting for the build squad — Vanguard FE, Ordnance BE, Logistics DB.

The record names our friction plainly: 4 of 6 tickets hit max effort, a11y bounced ×6 and tests ×5 — both inside the Inspector loop, both on the FE side, every one a defect we shipped instead of killed at the bench. Passes/ticket sits at 1.33, which flatters us; the real cost is the max-effort burns Vanguard ate re-fixing axe and coverage failures the Engineer's exit should have caught cold. The Drillmaster's hard gate — axe-core-clean AND non-negative coverage delta — landed this cycle but has **not** run a single ticket yet, so I have no telemetry to claim it works. I will not request a soldier on faith; padding the squad before the gate proves out just adds bench cost to the drag we're cutting.

Adjutant, concur fully on HOLD — and on your read that Scout (QA) is the true open seam, not a new FE hire. One ticket sits in `running` with no QA owner to receive it; that's a delivery dead-end waiting to happen, but it's next cycle's problem, not today's.

**Recommendation for today: HOLD the squad. Run the next FE ticket clean under the Drillmaster's new exit gate and instrument it — I report back whether a11y/test bounces actually drop to zero at the bench. If they don't, the fix is the gate's teeth, not more bodies.**

### Inspector General

S-2 Inspector General, reporting on quality and risk.

The record is unambiguous: a11y ×6 and tests ×5 are our systemic defects — every other category (regression, correctness, quality, performance) is a single isolated instance, statistical noise, not a pattern. Concur with Adjutant and Field Engineer on HOLD: the Drillmaster's axe-core-clean + non-negative-coverage gate is the correct origin-side fix, and recruiting against an unmeasured gate would only pad the drag. But hear my caution — the General's redundancy doctrine rests on my a11y/test auditors as "final verifiers," and that only holds if I instrument the next pass: I must confirm whether the gate drives bounces to zero at the bench or whether defects merely shift to where my auditors still catch them. Field Engineer, when you instrument the next FE ticket, tag a11y and coverage results pre- and post-gate — I will cross-check against my auditor findings so we know if the gate has real teeth or just moved the failure. The single quality risk to close this cycle is verifying the gate's effect, not adding standards; we do not tighten or ease until one clean ticket gives us data.

Recommendation for today: HOLD standards as-is and run one instrumented ticket — Field Engineer and I jointly report whether a11y/test bounces actually hit zero at the bench next council.

### Scout

S-2 Scout (Recon/QA), reporting on what actually breaks on DEV.

Plainly stated: this unit has **zero browser/e2e coverage** — every a11y ×6 and test ×5 bounce was caught by static auditors and axe-core in the bench, not by anything exercising the running app. That is our blind spot: axe-core clears the DOM at rest, but it never catches the defects that only appear in motion — keyboard focus traps in modals, focus order after route changes, dynamic content that never announces to screen readers, and the runtime errors that surface only when a real user clicks through a flow. The Drillmaster's gate makes the *component* axe-clean; it says nothing about whether the *assembled app on DEV* is operable. Field Engineer, Inspector — when you instrument the next ticket, your pre/post-gate tags measure static axe and coverage; they will not see a live focus-trap, so do not read a clean gate as a clean app.

The one smoke test worth standing up first: a **Playwright keyboard-only happy-path** through the ticket's primary FE flow — load, tab to every interactive control, operate it, assert focus is visible and never lost, and assert zero uncaught console errors. That single test attacks our exact recurring defect class (a11y in motion) where it actually lives, and it gives Scout a real verification step to receive the one ticket now sitting in `running`.

Concur with HOLD on the corps, Adjutant — but flag that the QA seam is a *coverage* gap, not just a roster gap.

**Recommendation for today: stand up one Playwright keyboard-and-console smoke test against the next FE ticket's primary flow on DEV — the first brick of Scout's QA post — and run it alongside Inspector's instrumented pass so we learn whether the axe gate survives contact with the live app.**

MEETING: live-QA smoke harness scope — Scout, Field Engineer, Inspector General.

### Provost Marshal

Provost Marshal (Security), reporting. The honest signal: in six tickets the unit has run **zero security gates** — gate failures sit at 0 because no authz, tenant-isolation, or secret-scan check has ever fired. That is the risk I carry: on a Supabase + FastAPI stack, tenant isolation lives in RLS policies and BE authz, and right now **nobody verifies them** before code lands on DEV. The Inspector's auditors catch a11y and tests; not one of them inspects a row-level-security gap, a missing tenant filter, or a secret committed to the client bundle — and Vite ships everything in `import.meta.env` straight to the browser, so a misplaced service key leaks to every tenant.

Scout — concur and extend: your Playwright happy-path should also assert the client bundle carries **no service-role key and no cross-tenant data**, because a focus trap is recoverable; a leaked Supabase service key is not. Field Engineer, Inspector — when you instrument the next ticket, tag one security axis alongside a11y/coverage: did any new endpoint or query run without a tenant scope?

**Recommendation for today: add a single hard security check to the Engineer's exit gate — secret-scan the diff (no keys/tokens) AND confirm every new Supabase query/endpoint carries an explicit tenant filter — before we stand up any new post. One origin-side control closes our largest unguarded seam at zero roster cost.**

MEETING: tenant-isolation + secret-scan exit check — Field Engineer, Inspector General, Provost Marshal.

### Quartermaster

Quartermaster, S-4 — Deploy readiness, reporting.

Every officer has named a build- or check-side gap, but mine is downstream and unguarded: in six tickets the record shows **not one verified DEV→MAIN promotion drill**. The mission ends "land on DEV, move to QA" — yet nobody owns whether DEV can actually ship clean. On this stack the landmine is **Supabase migrations**: a schema change applied to DEV that was never captured as a migration will pass every a11y, test, and security gate and still break MAIN on promotion, because MAIN's DB diverges silently. Provost Marshal — your Vite/`import.meta.env` secret-scan is my lens too; I extend it to **env parity**: a `VITE_`/FastAPI variable that exists on DEV but is undeclared for MAIN is a clean build that dies on deploy, and no current gate sees it.

Concur with Adjutant and Field Engineer on HOLD the corps — but readiness here is genuinely *unknown*, not green, and "gate failures: 0" reflects no deploy gate ever firing, not a safe path. The one readiness check to add at the Engineer's exit, at zero roster cost: **every DB schema change ships as a committed, reversible migration file, and every new env var is declared for MAIN — or the ticket does not land.** Verify on the next ticket that DEV's migration state and env manifest match MAIN before promotion; if they already diverge, we have a problem today, not next cycle.

MEETING: DEV→MAIN promotion readiness — migration + env parity gate — Quartermaster, Provost Marshal, Field Engineer.

### Drillmaster

Drillmaster, Doctrine & Training, reporting.

Adjutant, Field Engineer, Inspector — concur on HOLD and on instrumenting the next ticket; the gate I landed has zero tickets through it, so I will not stack new doctrine on an unmeasured fix and inflate the very passes/ticket we're cutting. But Scout's point is a genuine doctrine gap, not a roster gap: my exit gate makes the component axe-clean *at rest*, and per our own Lessons log a11y bounced ×6 — axe-core never sees a focus trap, a broken focus order after a route change, or a live region that never announces. That's the in-motion defect class re-entering through the same seam, and the cheapest place to kill it is the Engineer's bench, not Scout's future harness. So the ONE drill that compounds tomorrow at zero roster cost: a precise Knowledge edit to `frontend-engineer.md` — "axe-core verifies DOM-at-rest ONLY; for every interactive flow you build, author a keyboard happy-path test (tab to each control, operate it, assert focus visible and never lost, zero uncaught console errors)." That turns Scout's smoke test into a build-origin habit and gives the Inspector a concrete artifact to verify against. Provost's and Quartermaster's seams are real but unmeasured-and-different — security/deploy gates that have never fired — and per Commander's single-enforcement-point ruling I'd sequence those one cycle behind, after we confirm this gate's teeth.

No officer is overdue a refresher; no new recruit to onboard until Scout stands up.

**Recommendation for today: land one Knowledge line in `frontend-engineer.md` codifying the keyboard/focus/console happy-path test as a build-side requirement — killing a11y-in-motion at origin — and hold all other gate additions until the instrumented ticket gives us data.**

### Adjutant · r2

S-1 Adjutant, second round, from the roster.

Mark what just happened: Provost Marshal and Quartermaster each named a real unguarded seam — security/tenant-isolation and DEV→MAIN promotion — yet both proposed the fix as a *zero-roster-cost exit check*, not a new hire. That confirms my round-1 read: every gap surfaced today has a build-side owner at the Engineer's gate, so HOLD the corps is correct, not timid. Drillmaster, concur — your keyboard-happy-path Knowledge line turns Scout's smoke test into a build habit, which means we get the coverage before we spend a headcount on it. The one roster fact I'll flag: Scout, Provost, and Quartermaster sit at this table as advisory posts but their *verification steps are unbuilt* — three ownerless gates is more than one cycle can stand up cleanly, so they must be sequenced, not activated at once.

My sequencing call, per the Commander's single-enforcement-point doctrine: instrument the a11y/test gate on the next ticket first (Drillmaster + Field Engineer + Inspector own it), then stand up Scout's QA harness, with Provost's secret/tenant check and Quartermaster's migration/env parity following one cycle each behind.

**Recommendation for today: HOLD the corps — no new hire. Activate verification posts in sequence: Scout (QA) next cycle once the a11y gate proves out, then Provost (security exit check), then Quartermaster (promotion gate) — one per cycle so each new gate gets measured, not stacked blind.**

### Field Engineer · r2

S-3 Field Engineer, second round, for the build squad.

Five officers just routed new exit checks to my bench — Drillmaster's keyboard happy-path, Provost's secret/tenant scan, Quartermaster's migration/env parity, Inspector's pre/post tags, Scout's Playwright smoke. Adjutant, concur hard on your sequencing: stacking all five on a gate that has run **zero** tickets would spike passes/ticket and bury the one signal we need — does the a11y/test gate actually hold at the bench. Drillmaster, your keyboard/focus/console Knowledge line is the one build-side add I'll take this cycle, because it kills the exact recurring defect (a11y-in-motion, ×6) at origin and costs no roster — and it's an instrumentable artifact, not a new harness. Provost and Quartermaster, your seams are real and I will tag one security and one migration/env axis as **observation only** on the next ticket so we get telemetry without making them blocking gates yet — that respects the Commander's single-enforcement-point ruling while building your evidence for next cycle.

**Recommendation for today: take ONE build-side change — Drillmaster's keyboard happy-path Knowledge line — and instrument the next FE ticket with a11y/coverage as blocking plus security and migration/env as observation-only tags; I report the numbers next council so Provost's and Quartermaster's gates activate on data, not faith.**

### Inspector General · r2

S-2 Inspector General, second round, on quality and risk.

Scout and Drillmaster named the gap in my own verification doctrine, and I take it: my a11y auditors check axe-at-rest, so a focus trap in motion clears my gate today — that is a hole in *my* coverage, not just Scout's future harness. Field Engineer, concur on your blocking-plus-observation split: a11y/coverage blocking, security and migration/env as observation-only tags is the correct single-enforcement discipline — but hear the risk, an observation tag nobody cross-checks is noise, not evidence. So I commit: I will run my auditors against the pre/post-gate numbers AND adopt Drillmaster's keyboard-happy-path as a concrete artifact my a11y auditor verifies against, so we learn whether the gate killed the defect or merely moved it to where I still catch it. The single quality risk to close this cycle remains unchanged — prove the a11y/test gate has teeth on one instrumented ticket before we add or ease any standard; standards stay as-is.

Recommendation for today: HOLD standards — instrument one ticket with a11y/coverage blocking and Drillmaster's keyboard test as my verification artifact; Field Engineer and I report pre/post bounce numbers next council, and Provost's and Quartermaster's observation tags activate as gates only on that data.

### Scout · r2

S-2 Scout (Recon/QA), second round.

Drillmaster, Inspector — you both took the keyboard/focus/console happy-path and pulled it build-side; that's the right move and it means my smoke test becomes a build habit before Scout's harness even stands up. But mark the boundary plainly: a *unit-level* keyboard test in the Engineer's bench exercises a component in isolation — it still will not catch focus order breaking *after a route change* or a live region that fails only when the assembled app on DEV stitches two flows together. That cross-flow, in-motion defect is exactly what no auditor here sees today, and it's where the one ticket in `running` will get verified or won't. Field Engineer, concur with your blocking-plus-observation split — and I'll add: when you instrument, run the keyboard path against the **deployed DEV build**, not just the component, so we get one real datapoint on whether the gate survives assembly. Provost, your "no service key in the client bundle" assertion belongs in that same DEV-build smoke pass — I'll carry it.

**Recommendation for today: take Drillmaster's build-side keyboard line now, and run it once against the assembled app on DEV alongside Inspector's instrumented pass — that single live execution is the first brick of Scout's post and tells us whether axe-clean components add up to an operable app.**

### Provost Marshal · r2

Provost Marshal (Security), second round.

Field Engineer — concur on observation-only for the tenant-filter axis; that's a judgment call that needs telemetry before it blocks, and your split respects the Commander's single-enforcement ruling. But I push back on one point: secret-scanning the diff is **not** a measure-first axis. A focus trap is recoverable on the next pass; a Supabase service key shipped to `import.meta.env` is exposed to every tenant the instant it lands and is unrecoverable by rotation alone — you cannot un-leak it. That one check is binary, near-zero false-positive, and costs no passes/ticket drag, so it should be blocking now even while tenant-scope rides as an observation tag. Scout — accepted, you carry the "no service-role key, no cross-tenant data in the client bundle" assertion into the DEV-build smoke pass; that's the right place for the runtime half. Quartermaster — concur your env-parity is my lens extended; sequence it as observation alongside mine.

**Recommendation for today: split my ask — secret-scan the diff as a BLOCKING exit check now (irreversible exposure, zero drag), tenant-scope as an observation-only tag this cycle to build evidence; Scout carries the client-bundle no-key/no-cross-tenant assertion into the instrumented DEV smoke pass.**

### Quartermaster · r2

Quartermaster, S-4 — Deploy readiness, second round.

Provost Marshal, your binary/irreversible test is the right knife — and it cuts my ask the same way, so I'll apply it honestly. Env-parity is genuinely measure-first: a missing `VITE_`/FastAPI var is detectable and fixable before promotion, so it rides as observation-only alongside Field Engineer's split — concur. But the migration half is not symmetric: a schema change applied straight to DEV's Supabase with **no committed migration file** is silent divergence that rotation can't fix — you cannot reconstruct intent after the fact, and MAIN breaks blind on promotion. That "every schema change ships as a committed reversible migration" check is binary, near-zero false-positive, and zero passes/ticket drag — it belongs blocking now, exactly on Provost's secret-scan logic, while env-parity builds evidence as a tag.

Field Engineer — concur on instrumenting env-parity observation-only on the next ticket, but I need one hard datapoint today: does DEV's current migration state already match MAIN, or have the six landed tickets already diverged it? If they have, that's a problem this cycle, not next.

**Recommendation for today:** split my ask as Provost split his — **migration-file-exists-for-every-schema-change as a BLOCKING exit check now** (irreversible divergence, zero drag), **env-parity as an observation-only tag** this cycle; and verify on the next ticket that DEV↔MAIN migration state is in sync before any promotion drill.

### Drillmaster · r2

Drillmaster, Doctrine & Training, second round.

Scout — your boundary holds and it sharpens my drill, not weakens it: a unit-level keyboard test proves a *component* operable, so my Knowledge line must say so explicitly — "author the keyboard happy-path AND, where the flow crosses a route change or live region, assert it against the assembled DEV build, not the isolated component." That one clause keeps the build-side habit honest about its own ceiling and hands your future harness a defined seam instead of a blind spot. Field Engineer, Inspector — concur fully: one build-side change this cycle (the keyboard line, blocking) plus your blocking-plus-observation instrumentation is the disciplined load; stacking more would bury the signal. Provost, Quartermaster — your binary/irreversible knives (secret-scan, migration-file-exists) are not training gaps and not measure-first, so they sit outside my lens as straight exit rules; I'll codify whichever the Commander rules blocking into `frontend-engineer.md` next cycle once their teeth are set, per single-enforcement doctrine. No officer is overdue a refresher; no recruit to onboard until Scout stands up.

**Recommendation for today: land the keyboard happy-path Knowledge line in `frontend-engineer.md` with Scout's assembled-DEV-build clause for cross-flow cases — one instrumentable build-side drill that kills a11y-in-motion at origin; hold all other doctrine edits until the instrumented ticket gives us teeth-or-no-teeth data next council.**
