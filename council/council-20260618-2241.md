# Daily council — 2026-06-18 22:41

## The General's briefing

**SITREP** — 6 tickets run: 4 merged to DEV, 1 errored, 1 still `running`. Avg 1.33 passes/ticket; 4 hit max effort — the burn traces to the systemic a11y×6 / tests×5 recurrence. The Engineer's new exit gate (axe-clean + non-negative coverage delta) is live but has not yet completed a full cycle; the `running` ticket is its first instrument. The mission's terminal step — "QA on DEV" — still has no live owner, and the four DEV-ahead commits have never been run through Provost's secret-scan/cross-tenant SELECT or Quartermaster's env-parity audit. Council is unanimous: one variable moves this cycle.

**ORDERS FOR TODAY**
- **Adjutant:** Activate Scout (PLANNED→live) — one post only; hold Provost and Quartermaster at PLANNED with their checks riding the Commander's pre-flight per Unit Memory.
- **Drillmaster:** Onboard Scout — harden his Skills file with the keyboard happy-path smoke test as standing first-test, AND author the shared gate-comparable finding-record schema (ticket, flow, axe rule-id, coverage delta, pass/fail) all three verifiers log to. One doctrine artifact, not three edits.
- **Field Engineer:** Instrument the `running` ticket as the gate's proof case — log pre/post-gate axe + coverage numbers in Drillmaster's schema, and diff-tag the four DEV-ahead commits so downstream scans hit a frozen surface.
- **Scout (debut):** Stand up the keyboard happy-path smoke test on the `running` ticket's primary flow; scope strictly to runtime/a11y. **Inspector:** cross-check Engineer's pre/post numbers, Scout's catches, and his own auditors against that one ticket — report whether a11y/tests die at origin. Standards hold; no second build-side rule until the read is in.
- **Pre-promotion (Commander-executed):** Before any DEV→MAIN talk, run-and-log Provost's secret-scan + scratch-DB cross-tenant SELECT (zero rows) and Quartermaster's env-parity audit (every prod `/api/*` resolves to prod backend) across the four diff-tagged commits — prove the carry clean, don't assume it.

**FOR THE COMMANDER** — None.

## The record

```
Tasks run: 6
Outcomes: merged→dev: 4, errored: 1, running: 1
Avg passes/ticket: 1.33 | retried: 1 | hit max effort: 4
Gate failures: 0 | decisions needed: 0
Recurring Inspector issue areas: a11y ×6, tests ×5, regression ×1, correctness ×1, quality ×1, performance ×1
```

## Round-table

### Adjutant

**Adjutant — S-1, Personnel. Opening.**

Roster ground truth: nine officer files exist, but the README marks Scout (QA), Provost (Security), and Quartermaster (DevOps) as **PLANNED** — only Engineer, Inspector, and Drillmaster are live. The mission's terminal step is "QA on DEV," yet that step has no active owner, and the record shows one ticket `running` with nothing standing to receive it — a real, repeated capability gap, not a hypothetical. Scout's file is already drafted (Identity/Knowledge/Skills complete, independent verifier, memory-capped e2e + axe-core on DEV); this is an **activation**, not a recruitment, so it's cheap and reversible. The a11y×6 / tests×5 recurrence also tells me the build-side gate alone isn't yet closing the loop — a running-app verifier on DEV is exactly the second lens that catches what the diff-level Inspector can't see. I will *not* recommend any new recruit: every other gap (security, DevOps) is already owned on paper and unproven demand doesn't justify bodies.

**Recommendation for today:** Activate **Scout** as the next post — flip it from PLANNED to live so the currently-`running` ticket has an owner for the "QA on DEV" step; hold Provost and Quartermaster at PLANNED per the one-gate-per-cycle doctrine.

### Field Engineer

**Field Engineer — Builder. Opening.**

From the bench: four of six tickets hit max effort and avg passes/ticket sits at 1.33 — the burn isn't random, it's the a11y×6 / tests×5 recurrence forcing Inspector bounces before the new exit gate landed. That gate (axe-core-clean + non-negative coverage delta) is now live on `frontend-engineer.md` but has **not yet run a full cycle** — the currently-`running` ticket is our first real instrument of it, and I have no post-gate data proving it kills defects at origin versus shifting them downstream. Per our own doctrine, I will not add a second build-side rule until this one proves out. The one-ticket-`running`-with-no-receiver gap is real and it's mine to feel: my build completes and there's no QA owner on DEV to close the loop.

Adjutant — concur on activating **Scout**. A running-app verifier on DEV is the exact second lens my diff-level build can't self-provide; it gives the `running` ticket an owner and lets me cross-check a11y/tests against the *assembled* build, not just the patch.

**Recommendation for today:** Instrument the `running` ticket as the gate's proof case — tag axe-core violations and coverage delta pre- and post-gate, hand those numbers to the Inspector to cross-check his auditor findings, so next council decides corps changes on data, not a thin baseline. Hold all new build-side rules until that reads out.

### Inspector General

**Inspector General — Quality & Risk. Opening.**

The defect record is unambiguous: a11y×6 and tests×5 are the only *systemic* failures — every other category (regression, correctness, quality, performance) appears once, isolated noise. My auditors caught all of them, which means the check side is calibrated; the recurrence is a *build-origin* problem, now gated at the Engineer's exit. The live risk I own: that gate is unproven, and a diff-level review like mine is structurally blind to defects that only manifest in the assembled, running app — focus traps, broken live regions, route-change regressions. So the single quality risk to close is the **unverified-on-DEV seam**: we have no running-app lens, and one ticket is `running` with no verifier behind it.

Field Engineer — concur, and I'll commit to it: hand me pre- and post-gate axe/coverage numbers on the `running` ticket and I will cross-check them against my auditor findings. If the gate kills at origin, my a11y/test bounces should drop to zero on that ticket; if they don't, the gate is shifting defects downstream and we tighten at the bench, not add bodies. Adjutant — concur on activating **Scout**; a memory-capped e2e + axe-core pass on the assembled DEV build is exactly the second lens I cannot self-provide, and it gives that orphaned ticket an owner.

Standards hold, neither tighten nor ease this cycle — we have no post-gate data yet, and changing two variables at once destroys the read.

**Recommendation for today:** Activate Scout and instrument the `running` ticket as the gate's proof case; I will cross-validate gate numbers against my auditors and report whether a11y/tests die at origin before any further standard moves.

### Scout

**Scout — S-2, Recon (QA). Opening.**

Plain truth first: there is **zero browser/e2e coverage on DEV today** — Scout is PLANNED, so every one of the four merged tickets reached "done" without a single assertion against the assembled, running app. That is the unit's true blind spot, and it maps exactly onto the recurrence Inspector named: a11y×6 and tests×5 are precisely the defects that only surface at runtime — focus traps, lost focus on route change, dead live regions, console errors on interaction — which diff review and unit tests structurally cannot see. Adjutant, Field Engineer, Inspector — concur on activating Scout, and I'll be specific about what I receive: the `running` ticket is my proof case too, and I'll run the memory-capped axe-core + e2e pass on the DEV build so my findings cross-check against Field Engineer's pre/post-gate numbers and Inspector's auditors. The first smoke test to stand up is the **keyboard happy-path on the primary changed flow** (our standing FE requirement): tab every control, operate it, assert focus stays visible and never lost across any route change or live region, and assert zero console errors against the assembled DEV build — that single test would have caught the bulk of the a11y×6. I will not gold-plate the harness: one smoke test on the live flow this cycle, expand only on data.

**Recommendation for today:** Activate Scout and stand up the keyboard happy-path smoke test on the `running` ticket's primary flow as the unit's first DEV-runtime gate; report its catches against the build-side gate so next council judges on real runtime data.

### Provost Marshal

**Provost Marshal — Security. Opening.**

Plain truth from the record: four tickets merged to DEV and my two BLOCKING checks — secret-scan and the scratch-DB cross-tenant SELECT — have **never actually run**, because Provost is PLANNED. Unit Memory is explicit on this: "a defined BLOCKING check isn't a run check," and RLS policies can certify as idempotent/txn-wrapped yet still **fail open and pass every gate**. So the single biggest risk we carry is a **silent tenant-isolation gap on the DEV-ahead commits** — unverified RLS plus any leaked key, both irreversible once promoted, both currently riding on the *existence* of a gate nobody has executed. Adjutant, Inspector — I concur Scout activates first this cycle; runtime a11y is the proven recurrence and one-gate-per-cycle is sound. But Scout's e2e lens does not see RLS-fail-open or a committed secret, so this risk stays open after his activation.

The one control to add: a **hard pre-promotion block** — no DEV→MAIN until secret-scan and a cross-tenant SELECT returning zero rows have been *run and logged* across the DEV-ahead commits, even while Provost remains PLANNED; the Commander's own pre-flight owns that execution per Unit Memory until I stand up.

**Recommendation for today:** Before any promotion, run the secret-scan and a scratch-DB cross-tenant SELECT against the four merged commits and log the result — prove the carry is clean rather than assume it; Scout's activation proceeds in parallel, untouched.

### Quartermaster

**Quartermaster — S-4, Deploy Readiness. Opening.**

My lens is the one nobody else owns: can DEV actually promote to MAIN. Per Unit Memory, "promotion ≠ deploy" — landing on DEV is the mission's terminus, and a production cutover is a separate gate that the green build/typecheck never touch. The single biggest thing standing between DEV and a clean promotion is the **env-parity defect already on our record**: every prod `vercel.json` `/api/*` rewrite once pointed at the DEV backend while all gates stayed green. That gate is graduated to BLOCKING on evidence — but like Provost's checks, a *defined* gate isn't a *run* gate, and I have no log proving env-parity was executed across the four DEV-ahead commits. Provost Marshal — I'm fully aligned with your framing: your secret-scan and cross-tenant SELECT, and my env-parity audit, are the same class — irreversible-once-promoted controls riding on the mere existence of a gate, unrun. Scout's runtime lens (Adjutant, Scout — concur on activation) sees a11y, not a misrouted prod rewrite or a fail-open policy; those stay open after Scout stands up.

**Recommendation for today:** Before any DEV→MAIN talk, run and log the env-parity audit — assert every prod `/api/*` target resolves to the prod backend across the four merged commits — bundled with Provost's pre-flight as one "prove-the-carry-is-clean" pass; Scout activates in parallel, untouched.

### Drillmaster

**Drillmaster — Doctrine & Training. Opening.**

The record shows the real training event today is an *activation*, not a recurrence we drill out of existing officers — and onboarding the activated officer is my charge. Scout's file is drafted but has never run a cycle; a verifier who arrives without instrumented doctrine will produce findings that don't line up against Field Engineer's pre/post-gate numbers or Inspector's auditors, and we lose the read we all just agreed to take. So the single most compounding edit is to **Scout's Skills file**: codify the keyboard happy-path smoke test (tab every control, operate it, focus visible/never-lost across route changes and live regions, zero console errors on the assembled DEV build) as his standing first-test, and wire in the explicit cross-check protocol — log axe/coverage catches in a form directly comparable to the build-side gate. That converts Scout's first run from improvisation into a calibrated instrument tomorrow, and every cycle after.

Scout, Inspector, Field Engineer — concur, and that protocol is exactly what makes your three-way cross-check valid rather than three unaligned reports. Provost and Quartermaster: your pre-flight controls are gates the Commander executes, not training edits — I hold no drill there this cycle; one variable at a time. No refresher is due on the live three (Engineer, Inspector, me) — the exit-gate drill landed last cycle and is still proving out.

**Recommendation for today:** As Scout activates, I onboard him by hardening his Skills file with the keyboard happy-path smoke test as standing first-test plus the gate-comparable logging protocol — so his debut cycle is instrumented from minute one and feeds clean data to next council.

### Adjutant · r2

**Adjutant — S-1, Personnel. Round 2.**

Provost, Quartermaster — heard, and from the personnel lens your point strengthens mine rather than competes with it: your checks are real and irreversible, but Unit Memory already routes them to the Commander's pre-flight *while you remain PLANNED*. That is the correct posture — the controls run this cycle without standing up two more posts and blowing the one-gate-per-cycle doctrine. Your files stay drafted and ready; activation waits on Scout proving the activation *mechanism* before we repeat it for Security and DevOps. Drillmaster — concur, and this is the personnel point that matters: an activated officer without instrumented doctrine is a body, not a verifier; your Skills-file hardening is what makes Scout's activation count as added coverage rather than a third unaligned report.

No second personnel action this cycle — one activation, Scout, instrumented by Drillmaster, cross-checked by Inspector and Field Engineer.

**Recommendation for today:** Activate Scout only; hold Provost and Quartermaster at PLANNED with their checks riding the Commander's pre-flight, and re-examine their activation next council on Scout's debut data.

### Field Engineer · r2

**Field Engineer — Builder. Round 2.**

Drillmaster — concur hard, and this is the piece that makes my opening recommendation actually executable: my pre/post-gate axe and coverage numbers are only useful if Scout logs his catches in the *same comparable form*. Your Skills-file hardening is the schema that lets Inspector line all three reports up against one ticket — without it I hand over numbers nobody can join. Provost, Quartermaster — your env-parity and secret-scan/RLS checks ride the Commander's pre-flight, not my bench, but I own giving them clean commits to audit: I'll make sure the four DEV-ahead commits are diff-tagged so your scans run against a known surface, not a moving target. No new build-side rule from me this cycle — the exit gate is still proving out on the `running` ticket and I won't change two variables at once.

**Recommendation for today:** Hold my opening — instrument the `running` ticket as the gate's proof case with pre/post axe+coverage numbers in Drillmaster's gate-comparable format, so Scout's debut and Inspector's auditors cross-check against one shared baseline next council.

### Inspector General · r2

**Inspector General — Quality & Risk. Round 2.**

Provost, Quartermaster — you've named a risk my opening underweighted, and it's squarely my lens: an *unrun* BLOCKING check is indistinguishable from no check at all, and Unit Memory already warns these fail open while every gate stays green. Scout's runtime lens and my diff-level auditors are both structurally blind to a fail-open RLS policy or a misrouted prod rewrite — so those carry risks stay fully open after Scout activates, and I will not let "merge is clean" be read as "carry is clean." Drillmaster, Field Engineer — concur: the gate-comparable logging schema is precisely what makes my cross-check a join rather than three unaligned reports; without it I cannot certify whether the exit gate kills a11y/tests at origin. Standards hold this cycle — one variable moves (Scout), and I report the read before any further tightening.

**Recommendation for today:** Activate Scout and instrument the `running` ticket in Drillmaster's gate-comparable format for my cross-check; and before any DEV→MAIN, treat Provost's secret-scan + cross-tenant SELECT and Quartermaster's env-parity audit as *run-and-logged* preconditions on the four DEV-ahead commits — proven clean, not assumed.

### Scout · r2

**Scout — S-2, Recon (QA). Round 2.**

Provost, Quartermaster — heard, and I'll mark the boundary myself so no one mistakes my lens for cover: my e2e + axe pass on the assembled DEV build sees focus traps and dead live regions, it does **not** see a fail-open RLS policy or a prod `/api/*` rewrite aimed at the wrong backend. Those carry risks stay fully open after I stand up, and I won't let a green smoke test read as a clean carry — your run-and-logged pre-flight is a separate seam from mine. Drillmaster — concur hard: without your gate-comparable logging schema my keyboard happy-path catches are just a third unaligned report; with it, Field Engineer's pre/post numbers, Inspector's auditors, and my runtime findings join against one ticket. Field Engineer — your diff-tagged four-commit surface helps me too: it tells me exactly which assembled flow to drive first so I'm not smoke-testing a moving target.

**Recommendation for today:** Activate Scout and stand up the keyboard happy-path smoke test on the `running` ticket's primary flow in Drillmaster's gate-comparable format — and explicitly scope my lens to runtime/a11y only, leaving Provost's and Quartermaster's run-and-logged pre-flight as the separate, owner-executed carry-clean gate before any DEV→MAIN.

### Provost Marshal · r2

**Provost Marshal — Security. Round 2.**

Inspector, Scout — acknowledged, and your concessions close the loop I opened: you both now mark the boundary that an unrun BLOCKING check is indistinguishable from no check, and that your runtime/diff lenses are structurally blind to fail-open RLS or a misrouted prod rewrite. Quartermaster — fully aligned; your env-parity audit and my secret-scan + cross-tenant SELECT are the same class of irreversible carry control, and bundling them into one Commander-executed pre-flight is the right economy — one pass proves the four DEV-ahead commits clean. Field Engineer — your diff-tagged four-commit surface is exactly what I need: my scans run against a known, frozen surface, not a moving target, so I take that commitment. Adjutant — concur I stay PLANNED this cycle; the checks ride the Commander's pre-flight per Unit Memory, and that is the correct posture without standing up a second post against the one-gate doctrine.

The carry risk is unchanged and remains the unit's largest: unverified RLS plus any leaked key across the four merged commits, both irreversible once promoted.

**Recommendation for today:** Scout activates untouched; but no DEV→MAIN talk until the secret-scan and a scratch-DB cross-tenant SELECT returning zero rows are *run and logged* against the four DEV-ahead commits — bundled with Quartermaster's env-parity audit as one carry-clean pre-flight the Commander executes.

### Quartermaster · r2

**Quartermaster — S-4, Deploy Readiness. Round 2.**

Provost Marshal — fully aligned, and your round-2 framing closes my seam too: env-parity and your secret-scan + cross-tenant SELECT are one class of irreversible carry control, and a single Commander-executed pre-flight across the four DEV-ahead commits is the right economy. Field Engineer — I take your diff-tagged four-commit surface as the precondition that makes my audit valid: env-parity only means something against a frozen surface, so a moving target would force me to re-run every rewrite check. Adjutant — concur I hold at PLANNED this cycle; my env-parity check rides the Commander's pre-flight per Unit Memory, and standing up a second post against the one-gate doctrine buys nothing my check doesn't already get there. Scout, Inspector — heard and seconded: your runtime and diff lenses are structurally blind to a prod `/api/*` rewrite aimed at the DEV backend, so "merge clean" and "Scout green" never read as "carry clean" — that stays my open gate after Scout activates.

**Recommendation for today:** Activate Scout untouched; and before any DEV→MAIN, run-and-log the env-parity audit — assert every prod `/api/*` target resolves to the prod backend across Field Engineer's four diff-tagged commits — bundled with Provost's secret-scan + cross-tenant SELECT as one Commander-executed carry-clean pre-flight.

### Drillmaster · r2

**Drillmaster — Doctrine & Training. Round 2.**

Field Engineer, Inspector, Scout — concur, and your three endorsements sharpen my one edit into something I must get exactly right: the "gate-comparable format" you all want is a *shared schema*, and if it lives only in Scout's Skills file the join breaks the moment Field Engineer logs pre/post numbers in his own shape. So the drill is two-sided — I harden Scout's Skills file with the keyboard happy-path smoke test as standing first-test, *and* I write the same finding-record schema (ticket, flow, axe rule-id, coverage delta, pass/fail) as the common vocabulary all three reference, so Inspector's cross-check is a true join, not a reconciliation. That is one doctrine artifact, not three edits — it stays inside the one-variable-this-cycle line. Provost, Quartermaster, Adjutant — concur you ride the Commander's pre-flight at PLANNED; your controls are gates the Commander executes, not training files, so I hold no drill there and Scout's onboarding is my sole charge this cycle. No refresher due on the live three — the exit-gate drill is still proving out.

**Recommendation for today:** Onboard Scout by hardening his Skills file with the keyboard happy-path smoke test *and* authoring the shared gate-comparable finding-record schema all three verifiers log to — so his debut cycle produces a clean three-way join for next council, not three unaligned reports.
