# Daily council — 2026-06-16 17:08

## The General's briefing

**SITREP** — One ticket ran and merged to dev; that is the unit's sole completed operation on the record. It cost four Inspector passes, one retry, and three max-effort burns — unsustainable for a single build. A11y and tests failed on every single pass (×4 apiece) — predictable, preventable bounces. Root cause is confirmed structural: the Engineer's exit gate has no mandatory axe-core or coverage-delta stop, so a defective build can be handed off contractually. No gate broke; no decision currently sits open.

**ORDERS FOR TODAY**
- **Drillmaster:** Land the two `Exit gate` hard rules (axe-core-clean + non-negative coverage delta) in `frontend-engineer.md` before the next ticket opens. Leave `post-dev-checklist` untouched — single enforcement point, per standing guidance.
- **Engineer:** Adopt the new exit gate effective next ticket; no handoff or PR with a11y violations or a coverage drop.
- **Inspector squad (A11y, Tests):** Pivot from primary catch to final verifiers — the build-side gate now kills these defects at origin; you confirm, not cycle.
- **Adjutant:** No personnel action; hold the roster as constituted and re-examine passes/ticket after the next two tickets.
- **General:** Report passes/ticket next cycle to confirm the ×4/×4 bounce pattern collapses.

**FOR THE COMMANDER**
None.

## The record

```
Tasks run: 1
Outcomes: merged→dev: 1
Avg passes/ticket: 4.0 | retried: 1 | hit max effort: 3
Gate failures: 0 | decisions needed: 0
Recurring Inspector issue areas: a11y ×4, tests ×4, regression ×1, correctness ×1, quality ×1, performance ×1
```

## Round-table

### Adjutant

---

**ADJUTANT — S-1 PERSONNEL ASSESSMENT · 2026-06-16**

**Roster under review:** Engineer · Inspector · Inspector-Squad (A11y, Tests, Provost, Performance) · Drillmaster · Adjutant.

The single completed ticket burned four Inspector passes — a11y and tests each surfaced every round (×4 apiece). Both capabilities, however, already have owners: the A11y Auditor and Test-Coverage Auditor serve as the Inspector's specialist lenses and caught every instance. The gap is not on the check side; it is on the build side — the Engineer shipped without clearing those axes, a training deficiency the Drillmaster is actively closing with hard exit-gate rules. No ownership vacuum exists; no new billet is required. All officers are engaged: Drillmaster in training, Inspector enforcing, Engineer building, Adjutant assessing — no officer is idle or chronically underperforming from a one-ticket baseline.

**Today's recommendation:** **No personnel action.** Hold the roster as constituted, let the Drillmaster's hard-rule edit land on the Engineer, and re-examine passes/ticket after the next two tickets before any corps change is considered.

### Field Engineer

🤖 Model: Opus
⚙️ Effort: Low — briefing only, no code action

**FIELD ENGINEER — DELIVERY BRIEF · 2026-06-16**

One ticket shipped and merged to dev — that is the only win on the board, and it cost four Inspector passes to get there, with max-effort hit three times on a single build cycle; that is unsustainable burn rate for one ticket. The culprit is clear: the Engineer exited the build gate without clearing a11y and test coverage, forcing the Inspector to cycle repeatedly on defects that should have been killed at origin — a11y and tests each failed every single round. No gate formally broke, but three max-effort burns on one ticket signals the work was harder than scoped, the exit criteria were undefined at build time, or both.

The one change that cleans the next build: **the Drillmaster's hard-rule edit to `frontend-engineer.md` must land before the next ticket opens** — axe-core-clean and non-negative coverage delta as mandatory exit checks — so the Engineer cannot hand off a build that will bounce on those axes.

### Inspector General

---

**INSPECTOR GENERAL — QUALITY & RISK BRIEF · 2026-06-16**

The record is unambiguous: on the unit's sole completed ticket, a11y and tests each failed every single Inspector pass (×4 apiece) — every bounce was predictable and preventable. The Engineer's SOP (`engineer.md` Step 6 "Self-check") specifies only `tsc/lint` on changed files; it contains **no hard exit gate for axe-core or coverage delta**, which is the confirmed spec-gap — the Engineer is contractually permitted to hand off a build that will fail both axes, and did exactly that. What is slipping through is structural: defects that the Inspector's A11y and Test-Coverage auditors catch correctly every time, yet still enter the review cycle because the build side has no mandatory stop.

The single quality risk the unit must close is **the absence of mandatory axe-core-clean and non-negative-coverage-delta checks in the Engineer's Step 6 exit gate** — not a people issue, not a check-side gap, purely a missing hard rule on the build side. The Drillmaster's edit is approved by the Commander but, per this record, has not yet landed. Standards must **tighten** — specifically and only on that one enforcement point; the Inspector-squad's checks are calibrated correctly and require no change.

**Recommendation for today:** Confirm the Drillmaster's two hard-rule insertions into `engineer.md` are applied before the next ticket opens — this single change collapses the recurring ×4/×4 bounce pattern at source.

### Drillmaster

**DRILLMASTER — DOCTRINE & TRAINING BRIEF · 2026-06-16**

The record is unambiguous: a11y (×4) and tests (×4) failed on every single Inspector pass — not because the Engineer lacks knowledge (the file already documents both axes at lines 177–199), but because no hard contractual stop exists before handoff. The "Hard rules" block (line 209) contains seven `Never` rules; none of them blocks exit on a11y violations or coverage regression. The `When invoked — first ask` checklist (line 201) has five questions; none of them is a mandatory pass/fail gate. The Engineer is contractually permitted to ship a build that fails both axes, and did exactly that — four consecutive times.

**The ONE drill with maximum compounding gain:** Insert two lines into the `Hard rules` block of `/Users/romanberlin/.claude/agents/frontend-engineer.md`, immediately before "Never reimplement a primitive" (line 211):

```
- **Exit gate — A11y:** Run `axe-core` (or `axe-playwright`) before every handoff. Zero violations is mandatory. Do not open a PR, do not hand to Inspector if violations exist.
- **Exit gate — Tests:** Coverage delta must be ≥ 0 on every PR (no net loss). Run coverage report before handoff and confirm it. If coverage drops, add tests — do not hand off.
```

These two lines transform a knowledge rule into a contractual stop. Every future ticket the Engineer touches will check both axes at source — collapsing the ×4/×4 bounce pattern before it reaches the Inspector.

**Today's recommendation:** Commander authorizes Drillmaster to land the two `Exit gate` lines in `frontend-engineer.md` Hard rules block before the next ticket opens.
