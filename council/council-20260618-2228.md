# ship-review: automatixy — 2026-06-18 22:28

## The General's briefing

**VERDICT** — GO WITH CAVEATS for DEV→MAIN as an *integration* milestone; NO-GO for a production *deploy* until the items below clear.

**BLOCKERS** — (must be fixed before any prod release off MAIN; none block the integration merge)
- **vercel.json env-misroute** — every prod-bound frontend (`crm`, `superadmin`, `form`, `microsite`) rewrites `/api/*` → `zelpath-backend-dev.onrender.com`. This is cross-environment exposure: prod traffic and user data would transit a dev-grade backend. Invisible to build/typecheck (both green). Parameterize per-environment and re-verify.
- **Tenant-RLS posture unverified** — `feat/tenant-scoped-hooks` drops/recreates RLS policies and disables two audit triggers. Migrations land safely (QM certified), but no one has proven the *resulting* policies deny cross-tenant reads. A fail-open policy stays green through every gate. Provost's scratch-DB cross-tenant SELECT (zero rows) must pass before prod.
- **Secret-scan not run** — Provost's BLOCKING exit check has not been run across the ~40 DEV-ahead commits. A leaked Supabase key can't be un-leaked; this gates regardless of passes.

**PRE-FLIGHT** — (Commander verify before promoting / before cutover)
- Run Provost's secret-scan clean across all DEV-ahead commits.
- Run Provost's tenant-RLS verification: every tenant-scoped table has an active policy; cross-tenant SELECT returns zero rows.
- Parameterize and re-verify env-parity: all prod `vercel.json` `/api/*` targets resolve to the prod backend, not dev.
- Confirm the owner-gated legal items (`DEPLOYMENT_GATES.md`: policy v1.0 seed + attorney attestation, consent rollout) — yours alone to sign.
- Note the unproven build-side exit gate: the one `running` ticket hasn't completed under the new axe-core/coverage gate; a11y (×6) and tests (×5) are our systemic FE defects. Instrument pre/post-gate before relying on it.
- Pin backend deps (`google-genai`, `httpx`, `python-dotenv` — no lockfile) and add `tsc --noEmit` to CI; soon, not blocking.

**FOR THE COMMANDER** — Code compiles, types and migrations are clean — the integration merge is defensible, but a production cutover is not, on technical grounds alone, until the misroute, RLS, and secret-scan clear and the legal gates are signed. Promote DEV→MAIN? Your call.

## The record

```
Tasks run: 6
Outcomes: merged→dev: 4, errored: 1, running: 1
Avg passes/ticket: 1.33 | retried: 1 | hit max effort: 4
Gate failures: 0 | decisions needed: 0
Recurring Inspector issue areas: a11y ×6, tests ×5, regression ×1, correctness ×1, quality ×1, performance ×1

Quartermaster readiness report:
I have enough to certify. Backend compiles clean; all typechecks/build pass; migrations are well-engineered. Here's my report.

---

# 🎖 QUARTERMASTER — DEPLOY-READINESS REPORT · `automatixy`

**Branch inspected:** working tree on `feat/tenant-scoped-hooks` (1 commit ahead of `DEV`); `DEV` is ~40 commits ahead of `MAIN`.
**Promotion in question:** `DEV → MAIN`. MAIN tracks production.

## VERDICT: ✅ TECHNICALLY READY to promote `DEV → MAIN` · ⚠️ NOT CLEAR for production *deploy* (owner-gated)

There are **no hard technical blockers** to landing DEV on MAIN as an integration milestone. The code compiles, typechecks clean, migrations are safe, and the lockfile is honest. **But MAIN tracks production, and an actual prod deploy off MAIN is blocked** by (a) documented legal/attestation gates that are the Commander's call, and (b) one deploy-config item (vercel backend target). Promote the branch if you want; do **not** trigger a prod release until the gates below clear.

---

## Verifications run (read-only, memory-safe)

| Check | Result |
|---|---|
| **Build** — `vite build` zeltivo-crm (touched app) | ✅ pass (3.27s; large-chunk warning only) |
| **Types** — `tsc --noEmit` × 6 apps (crm, zelmero, superadmin, form, landing, microsite) | ✅ all clean (exit 0) |
| **Backend** — `compileall backend/src` | ✅ clean |
| **Dependencies** — `bun install --frozen-lockfile --dry-run` | ✅ lockfile in sync |
| **Migrations** — 12 new in DEV vs MAIN, inspected destructive ones | ✅ idempotent, guarded, txn-wrapped, rollback files present |

The migrations are **exemplary** — the status-remap (`20260610180000`) is idempotent (`WHERE … IN (legacy)`), transaction-atomic, disables only the two audit triggers by name (not `session_replication_role`), and the policy DROPs are all `IF EXISTS` with `_rollbacks/` counterparts. No unguarded `DROP`/`TRUNCATE`/`DELETE`. Migrations target the single live Supabase project out-of-band, so they are not coupled to the MAIN promotion itself.

---

## Blockers & warnings

### 🔴 Hard blocker — for PRODUCTION deploy only (not for DEV→MAIN merge)
1. **All frontend `vercel.json` rewrites point `/api/*` → the DEV backend** (`zelpath-backend-dev.onrender.com`) — zeltivo-crm, superadmin, zeltivo-form, microsite.
   - *Where:* `apps/*/vercel.json`
   - *Impact:* if a prod Vercel project deploys off MAIN with these files, prod frontends call the DEV backend. Correct for the current pre-launch/DEV-staging posture (docs say the prod backend `automatixy-backend-prod` provisions separately at launch), wrong the moment prod goes live.
   - *Fix:* parameterize the rewrite target per Vercel environment before prod cutover; track against the pre-launch checklist.

### 🟡 Owner-gated (surfaced, not mine to clear)
2. **Legal release gates** (`Documentation/DEPLOYMENT_GATES.md`): the `policy_documents` v1.0 seed + attorney attestation, and the zeltivo-crm/form consent rollout, are unsigned. CLAUDE.md standing orders: do **not** flip `VITE_CONSENT_MODAL_ENABLED`, seed v1.0 policy text, or apply migrations to the live project without the checklist. These gate *production*, not the DEV→MAIN merge.

### 🟡 Warnings (don't block, fix soon)
3. **Backend deps unpinned** — `backend/requirements.txt` uses `>=` ranges and fully-unpinned `google-genai`, `httpx`, `python-dotenv`. A fresh Docker build can pull a breaking upstream release. No lockfile.
4. **CI doesn't typecheck** — the `build` script is `vite build` (esbuild strips types); no `tsc --noEmit` s
```

## Round-table

### Inspector General

🎖 INSPECTOR GENERAL — opening, quality & risk lens.

The record is consistent and unambiguous: a11y appears in **all 6 tickets**, tests in **5 of 6** — these are not noise, they are the unit's two systemic FE defects, every other category (regression, correctness, quality, performance) is a single isolated instance. Our memory already names the fix: the Engineer's hard exit gate (axe-core-clean + non-negative coverage delta) landed this week — but it is **unproven**; no full ticket has yet run end-to-end under it, so I cannot certify the defect is dead at the bench rather than merely shifted downstream to where my auditors still catch it. That is the single quality risk for this council: **we are being asked to promote to production before our newest gate has caught a single defect in flight.**

On the promotion question itself, I align with the Quartermaster's split verdict but read it through risk: his 🔴 vercel.json finding — every prod frontend's `/api/*` rewrite still points at the DEV backend — is exactly the class of correctness/regression defect that does not show up in typecheck or build (both green) and would only surface in production. Clean compiles are not evidence of prod-readiness here. Standards should hold, not ease: DEV→MAIN as an integration milestone is defensible, but a production *deploy* is not, on technical grounds alone, independent of the owner-gated legal items.

My recommendation for today: **do not promote to MAIN as a production cutover.** Treat DEV→MAIN as integration only, and gate any prod deploy on (1) the vercel.json rewrite being parameterized per-environment and re-verified, and (2) the Engineer's new exit gate proving out on the one ticket still `running` — instrument it pre/post-gate so we confirm the kill happens at build origin.

MEETING: prod-deploy readiness — Quartermaster (vercel config + deps), Drillmaster (verify the new exit gate on the running ticket), Inspector (cross-check a11y/coverage pre/post-gate).

### Provost Marshal

🎖 PROVOST MARSHAL — security & exposure lens.

The single biggest risk I read is not in the green typechecks — it's that this branch, `feat/tenant-scoped-hooks`, **rewrites RLS policy (multiple `DROP POLICY IF EXISTS`) and disables two audit triggers**, yet nothing in the Quartermaster's certification verifies the *resulting* tenant-isolation posture. His report proves the migrations are idempotent, reversible, and txn-wrapped — that they land safely, not that the policies left standing actually deny cross-tenant reads. On a zero-trust multi-tenant app, a dropped-and-recreated policy that's subtly wrong fails open and silent: build, typecheck, and `compileall` all stay green while one tenant can read another's rows. Inspector General — your vercel.json finding is worse through my lens than correctness: prod frontends calling the DEV backend means **production user data transits and lands in a dev-grade environment**, a cross-environment exposure, not just a misroute.

Two doctrine items also ride before any MAIN promotion: per our 2026-06-18 standing order, my **secret-scan is a BLOCKING exit check** and the record shows no scan was run on these ~40 DEV commits — a leaked Supabase key can't be un-leaked, so it gates regardless of passes. The unpinned backend deps (`google-genai`, `httpx`, `python-dotenv`, no lockfile) are a real supply-chain hole but lower-immediacy — they bite a future Docker build, not this merge.

My recommendation for today: **before DEV→MAIN, I run a secret-scan across the new commits AND a tenant-RLS verification on a scratch DB — assert every tenant-scoped table has an active policy and a cross-tenant SELECT returns zero rows.** DEV→MAIN as integration is fine; production deploy stays blocked until that scan is clean. I'll join the huddle Inspector called.

MEETING: prod-deploy readiness — add Provost to run secret-scan + tenant-RLS verification alongside Quartermaster, Drillmaster, Inspector.

### Quartermaster

🎖 QUARTERMASTER — S-4, deploy-readiness lens.

My certification stands and I'll sharpen it: **DEV→MAIN as an integration milestone has no hard technical blocker, but the single thing between DEV and a clean *production* cutover is the `vercel.json` rewrite — every prod-bound frontend (`crm`, `superadmin`, `form`, `microsite`) routes `/api/*` to `zelpath-backend-dev.onrender.com`.** Build and typecheck stay green through this because it's runtime deploy config, not code — exactly the blind spot Inspector named. Provost, you read it correctly and worse than I scored it: this isn't a misroute, it's production traffic and user data transiting a dev-grade backend — a cross-environment exposure, not a 404. I concede the upgrade; it rides as a blocking prod-deploy check, not a warning.

On the migrations — Provost, I certified they *land* safely (idempotent, txn-wrapped, reversible); I explicitly did **not** certify the resulting RLS posture, and you're right that a dropped-and-recreated policy can fail open while every gate I run stays green. That's your scratch-DB cross-tenant SELECT, and it's outside my lens — I support it gating prod. Inspector, the running ticket and the unproven exit gate are real but they're build-bench concerns; they don't touch the DEV→MAIN config readiness, so don't let them hold the integration merge.

The one readiness check to add, per our gate-activation doctrine: an **env-parity assertion** — every prod-bound `vercel.json` `/api/*` target resolves to the prod backend, not dev. Because it's a deterministic, irreversible-if-shipped config check (like migration-file-exists), it rides **blocking for prod cutover immediately**; the probabilistic side stays observation-only.

Recommendation for today: **certify DEV→MAIN as integration; hard-block any prod deploy until the vercel.json targets are parameterized per-environment and re-verified.** I'll bring the env-parity check and the backend-deps pinning gap to the huddle.

MEETING: prod-deploy readiness — Quartermaster (vercel env-parity + deps), Provost (secret-scan + RLS), Drillmaster (exit gate), Inspector (a11y/coverage pre/post-gate). I attend.
