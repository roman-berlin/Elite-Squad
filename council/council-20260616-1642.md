# Daily council — 2026-06-16 16:42

## The General's briefing

**SITREP** — One ticket entered the pipe and merged to dev; the build moved forward. It cost 4.0 Inspector passes, one retry, and three hits at the effort ceiling. The drag came almost entirely from two axes: accessibility and test coverage, each flagged four times on that single diff. Correctness, regression, performance, and quality registered once apiece — backstop functioning, not alarming. Zero gate failures, zero decisions escalated. The council is unanimous: the defect is upstream, at the Engineer's exit gate, not in the Inspector's squad.

**ORDERS FOR TODAY**
- **Drillmaster** — execute `general drill` now: edit `/Users/romanberlin/.claude/agents/frontend-engineer.md`, adding to `## Hard rules` immediately before the keyboard/dark-mode/mobile line two hard exit criteria: (1) no diff submitted without a zero-violation axe-core scan, (2) no diff submitted without a non-negative branch-coverage delta. This converts aspirational doctrine into an enforced gate.
- **Field Engineer** — adopt the new pre-submission checklist as exit criteria, not Inspector concerns: axe-core clean and coverage delta verified before any diff leaves your hands.
- **Inspector** — hold squad standards exactly as calibrated; pivot the a11y and test auditors from first-pass catchers to final verifiers once the Engineer gate is live. Report next cycle whether passes/ticket drop.
- **Adjutant** — roster stays full and unchanged; no recruitment. The fix is a gate rule, not headcount.

**FOR THE COMMANDER**
Should the two exit criteria (clean axe-core, non-negative coverage delta) also be injected into the `post-dev-checklist` skill's Step 3 as peer-enforced checks, or should the Engineer remain the sole enforcement point to avoid duplicate gate costs?

## The record

```
Tasks run: 1
Outcomes: merged→dev: 1
Avg passes/ticket: 4.0 | retried: 1 | hit max effort: 3
Gate failures: 0 | decisions needed: 0
Recurring Inspector issue areas: a11y ×4, tests ×4, regression ×1, correctness ×1, quality ×1, performance ×1
```

## Round-table

### Adjutant

**S-1 ADJUTANT — PERSONNEL ASSESSMENT · 16 JUN 2026**

---

**Roster status: FULL and correctly constituted.** All four posts — Engineer, Inspector (with embedded check-squad: Provost, A11y Auditor, Performance Auditor, Test-Coverage Auditor), and Drillmaster — are manned and scoped appropriately for the pipeline in front of us.

**The record raises one clear signal:** the Inspector's squad is working hard — a11y and tests each flagged 4 times on a single merged ticket, with performance and correctness each appearing once — which drove the high pass-rate of 4.0 and three effort-ceiling hits. That is not a roster gap; the auditors exist and are catching real issues. The root problem is upstream: the Engineer is delivering diffs that are not a11y- and test-complete on the first pass.

**No capability gap exists without an owner** — every audit lens (A11y, Tests, Security, Performance) is already staffed inside the Inspector's check-squad. Recruiting a new officer would add headcount without resolving the root cause.

**RECOMMENDATION FOR TODAY:** Direct the Drillmaster to run `general drill` immediately — the recurring a11y ×4 / tests ×4 pattern is a textbook recurring weakness and the Drillmaster exists precisely to turn it into a permanent Engineer instruction before the next ticket enters the pipe.

### Field Engineer

🤖 Model: Opus
⚙️ Effort: Low — single-lens delivery debrief, no code changes

---

**FIELD ENGINEER — DELIVERY REPORT · 16 JUN 2026**

One ticket shipped and merged to dev — the build moved. However, the record is damning on process: 4.0 passes per ticket, three effort-ceiling hits, and the Inspector's squad cycling through a11y and test deficiencies four times each on a single diff. That means the Engineer submitted code that was visibly incomplete on accessibility and test coverage, and the pipeline burned four correction loops to compensate — a direct tax on build velocity.

The friction is not mysterious: a11y compliance and test completeness are being treated as Inspector concerns rather than Engineer exit criteria. Every cycle the Inspector catches a missing ARIA label or an untested branch is a pass the Engineer should have burned before submission. Three hits at max effort indicate the corrections were also non-trivial — these were not typos, they were structural gaps.

The codebase is not the enemy here; the checklist is. The Engineer's pre-submission gate has no enforced a11y or coverage bar, so deficiencies flow downstream and detonate in the Inspector's squad repeatedly.

**RECOMMENDATION FOR TODAY:** Direct the Drillmaster to codify a hard pre-submission checklist item — no diff leaves the Engineer's hands without a passing axe-core scan and a branch-coverage delta check — so the Inspector's squad pivots from catching first-pass failures to confirming quality that was already verified upstream.

### Inspector General

**INSPECTOR GENERAL — QUALITY & RISK ASSESSMENT · 16 JUN 2026**

---

The record is unambiguous: on a single merged ticket, a11y and test deficiencies each triggered **four inspector interventions** — accounting for every gate pass beyond the first and driving three max-effort ceiling hits. These are not edge catches by a vigilant squad; they are **systematic first-pass failures** in two foundational quality dimensions that the Engineer's submission gate is not filtering. Correctness and performance each registered once, confirming the Inspector's squad is functioning correctly as a backstop — but a backstop being struck four times on the same axis is a standards failure upstream, not a coverage success.

What is slipping through: the pre-submission contract has **no enforced accessibility bar and no coverage delta requirement**. Structural a11y gaps (not typos — three max-effort corrections confirm this) and untested branches are reaching the Inspector's queue as live defects, not pre-flagged risks. The regression signal (×1) and correctness signal (×1) are tolerable at this frequency; the a11y/test recurrence is not.

The **single quality risk this unit must close**: the Engineer's exit gate has no hard axe-core or branch-coverage checkpoint, making the Inspector's a11y and test auditors act as first-pass catchers rather than final verifiers. Until that gate hardens, every ticket will burn 3–4 passes and ceiling the effort tier — a compounding velocity tax.

**Standards must tighten** — specifically and only at the Engineer's pre-submission boundary. The Inspector's squad standards are already calibrated correctly; loosening them would let real defects ship.

**RECOMMENDATION FOR TODAY:** Direct the Drillmaster to issue a standing Engineer drill encoding two hard exit criteria — axe-core scan must pass clean, and branch-coverage delta must be non-negative — before any diff is submitted for inspection. No new officers needed; the fix is a gate rule, not a headcount addition.

### Drillmaster

**DRILLMASTER — DOCTRINE & TRAINING REPORT · 16 JUN 2026**

---

The record speaks without ambiguity: a11y flagged **four times**, tests flagged **four times**, on a single ticket — every correction loop was a tax the Inspector should never have paid. Inspection of the `frontend-engineer.md` agent confirms the diagnosis: a11y and test coverage appear in "Non-negotiables" and "Hard rules" as *aspirational doctrine* (lines 177–199), but the **"When invoked — first ask" intake gate** (lines 201–208) lists five questions — none of which are exit criteria, and none of which enforce `axe-core` or coverage before the diff leaves the Engineer's hands. The `post-dev-checklist` skill defers a11y to "Gate 13 / accessibility-audit skill," which means the Inspector absorbs what the Engineer should have burned. The gap is structural: doctrine exists, enforcement does not.

**THE ONE DRILL:** Add two lines to the `## Hard rules` section of `/Users/romanberlin/.claude/agents/frontend-engineer.md`, immediately before the line `Never ship without keyboard + dark-mode + mobile verification`:

```
- Never submit a diff without a zero-violation axe-core scan (`bun test -- --grep a11y` or equivalent).
- Never submit a diff without a non-negative branch-coverage delta (`--coverage` output must show no regression).
```

This single edit converts aspirational policy into a hard exit gate at the source, compressing four Inspector passes into zero — the highest-yield doctrine change available in the record today.

FOR THE COMMANDER: Should these two exit criteria also be injected into the `post-dev-checklist` skill's Step 3 quality gates as peer-enforced checks, or does the Commander prefer the Engineer remain the sole enforcement point to avoid duplicate gate costs?
