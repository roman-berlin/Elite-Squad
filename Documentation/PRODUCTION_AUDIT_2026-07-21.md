# Production-readiness audit — 2026-07-21

*16-agent workflow (8 dimensions × finder + skeptic), 2.08M tokens, run against the live repo+state.*
*P0s fixed same-day on dev (see the `fix(prod)` commit); P1/P2 deferred below. Matrix truncated to titles+scenarios; full evidence in the workflow transcript.*

## crash-recovery
Crash & restart resilience audit of /Users/romanberlin/Projects/General. What is SOLID: state stores are atomic (locking.py tmp+fsync+os.replace; audit.jsonl is flock-serialized append), worktree locks are flock (OS-released on death, loop.py:702-726), the reaper self-excludes canonical and current worktrees (git_ops.py:55-168), the PID file carries pid+start identity (autopilot.py:325-401), and EU-385 boot resume demonstrably works. LIVE TRACE of the 23:05:05 pair: AUTO-80 started 15:42:51 in serve pid 91892; builder iteration 1 finished ok:false at 16:23:02 ($5.44, 6.03M builder tokens after a $1.34 planner pass); last event 16:23:57 backlog_unreachable (DNS dead) — the MACHINE then went down (no launchd respawn for 6.7h; the 23:05 process has pid 950, a boot-time pid, so this was a Mac …

- **[P0] Mid-land crash window: process death between the dev push and the Jira transition/changelog leaves NO durable merged-marker, and the restarted drain re-picks and rebuilds already-merged code**
  - scenario: Process dies (power cut, OOM SIGKILL, machine sleep) in the seconds between the push and the changelog write. Boot resume (EU-385) restarts the drain, Tier-1 re-picks the still-In-Progress ticket, replans and rebuilds on top of its own merged code: at best a full duplicate spend (AUTO-80 scale: ~$7-10 and ~40 min), at worst the builder lands a junk/no-op second merge and the ticket gets a second, 
  - fix: Write one durable marker IMMEDIATELY after land_trial succeeds, before any network/LLM step: record the 'merged' audit event (move loop.py:2887 up to right after 2805) and/or persist recently_merged to a state file (same locked_rmw pattern as error_counts). Then let _already_landed accept (merged audit event + origin/<base> commit) as the second key when the changelog line is absent, so the autocl
- **[P1] EU-387 self-update restart never fires without a running drain: a self-repo land from a manual cockpit run announces "restarting automatically once idle" but nothing ever consumes the flag**
  - scenario: Commander runs an EU ticket via the cockpit ticket-picker while no drain is armed (a normal workflow per the drive-unit-via-cockpit practice). The land changes the unit's own code, Telegram says it will restart automatically, and the serve process keeps executing the OLD code from memory indefinitely — the exact stale-process class that ran pre-EU-201 code for 11h and stranded split fragments (the
  - fix: Add a serve-level consumer: a small periodic check in the cockpit process (e.g. the existing SSE heartbeat timer or a dedicated thread) that calls _maybe_self_restart when active_run_count()==0 — the same safety checks already gate it. Alternatively, make the loop.py:2901 notify conditional: when no drain intent is RUNNING, send the 'RESTART BY HAND' variant instead of promising an automatic resta
- **[P1] No crash-loop breaker on boot resume: a repeatable mid-build process kill (macOS OOM class) produces an unbounded respawn -> auto-resume -> rebuild -> kill cycle, burning a full planner+builder spend per iteration**
  - scenario: A ticket whose build repeatably drives the host out of memory (or trips any process-fatal bug) kills serve mid-build. KeepAlive respawns in 5s, EU-385 re-arms the drain, Tier-1 re-picks the same ticket, the build runs ~30-40 min and dies again. Overnight this loops dozens of times: hundreds of dollars / tens of millions of tokens burned, audit full of process_start pairs, no per-ticket strike, no 
  - fix: In resume_armed_drains, count recent autopilot_resume events for the same app key in the audit tail (the file is already being scanned for autopilot_stop_requested); after N resumes within M hours with no intervening clean autopilot_stop, keep the intent but SKIP the resume and alert loudly ('crash loop suspected — resume held for the Commander'). Optionally also persist a 'ticket in flight' marke
- **[P2] Mid-scrum-split crash duplicates the split: children are filed but the parent stays open, and the retry files a second Epic + children with no dedup**
  - scenario: Process dies during the ~5-15s of Jira creates in a split (a drain restart, machine sleep). Boot resume re-picks the still-open parent, planner verdicts SPLIT again, and the board gets two parallel Epics with near-identical children. The drain then builds both sets: duplicate branches, duplicate merges of the same feature slices, conflicting code on dev, and double spend for every fragment.
  - fix: Before filing, have scrum.split query the backlog for open auto-split children referencing parent.id (the '— Auto-split from <id>' body marker is already machine-greppable, and Epics link children via parent). If found, adopt the existing keys (return ok=True with them, close the parent) instead of re-filing.
- **[P2] Crash (or ff-divergence) between the changelog append and its git commit leaves the primary checkout permanently dirty, which then blocks BOTH the EU-386 self-update restart and the autopull fast-forward — the stale-cockpit class**
  - scenario: A land appends the changelog; the process dies before the commit (or the commit skips on divergence). Every subsequent boot warns EU-386 but nothing heals. The next self-repo land flags a restart, the drain reaches its cycle boundary, tree_forensics reports dirty, the flag is deleted and the unit permanently keeps running old code while autopull is simultaneously blocked — two independent stalenes
  - fix: At serve boot (next to warn_dirty_tree in main.py:454), self-heal the one known-safe case: if git status shows ONLY Documentation/Development_Status.md modified, run the existing _git_commit_changelog. Also make _maybe_self_restart exempt that single path from the dirty-tree refusal (it is machine-written release hygiene, not un-gated WIP).
- **[P2] Manual cockpit runs (/api/run-selected, /api/run choose-mode) have no crash coverage: a mid-build death strands the ticket In Progress with nothing to resume it and no alert**
  - scenario: Commander starts a single-ticket cockpit run and the process dies mid-build (the 2026-07-19 deploy-restart incident killed AUTO-177 exactly this way). After respawn the board shows the ticket In Progress, the cockpit shows nothing running, no notification says the run died, and the ticket sits until a human notices or a drain for that app is manually started (whose Tier-1 would then rescue it).
  - fix: On serve boot, reconcile: after resume_armed_drains, scan the audit tail for run_start events without a matching run_end from dead pids (or persist a lightweight 'manual run in flight: app+tickets' file at claim, cleared in the _bg finally) and send one Telegram alert naming the stranded tickets — 'run died mid-build; ticket X is still In Progress, restart it or start a drain'.
- **[P2] A machine-level crash/reboot is invisible to the unclean-restart alert because the PID file lives in /tmp, which macOS wipes at boot**
  - scenario: Any hard reboot / power loss mid-drain: the respawned unit auto-resumes but the audit contains no unclean-shutdown record, so forensics (and the daily brief) undercount crashes and the 'restart is never silent' invariant silently fails for the most violent failure mode. Paired risk: the 6.7h dead gap had zero alerting while down — nothing external watches the cockpit's liveness.
  - fix: Move the PID file (or a duplicate boot-marker) into state/ (e.g. Path(cfg.audit_path).with_name('autopilot.pid')), which survives reboots — _read_pid_record's pid+start identity check already handles pid recycling after reboot correctly. Keep GENERAL_PID_FILE for tests. Separately, resume_armed_drains can detect 'intent RUNNING but no stale pid record' as the machine-reboot signature and record an

## state-integrity
State-store integrity audit of /Users/romanberlin/Projects/General/state/. Inventory and verdicts: audit.jsonl — EU-363 rotation is IMPLEMENTED and verified live (audit.py:46-47 5MB threshold / 14-day window; archives state/audit/archive-2026-06.jsonl + -07.jsonl written Jul 19; live file now 4.07MB/11,402 lines, well under threshold; archive-first-truncate-after crash ordering at audit.py:123-133; dashboard's EU-345 incremental reader handles the truncation via size-decrease + anchor checks, dashboard.py:129-191) — the EU-363 'never rotates' claim is stale, hot-path growth is bounded. usage_ledger.jsonl (472KB) is pruned to 35 days at every CLI/serve boot (main.py:439, usage.py:690-703 via locked_rewrite, race-safe against concurrent appends). logs/ purges 30-day-old folders (config.yaml:…

- **[P1] needs_sync.reconcile blind-overwrites blocked_tickets.json with a snapshot taken before a minutes-long Jira scan — re-introduces the EU-218 lost-park class the drain itself already fixed**
  - scenario: Roman opens /needs at 14:00:00 → background reconcile snapshots parked={EU-A}. The drain finishes its cycle at 14:00:05 and parks EU-X with the Commander's open question (autopilot.py:2123-2128, save_blocked succeeds). Reconcile finishes its Jira scan at 14:00:12, finds EU-A Done, and writes still_parked={} — EU-X's park is erased with no audit trail. Next cycle the drain re-picks EU-X, re-runs th
  - fix: Make the clear operation a true read-modify-write: add `autopilot.remove_blocked(cfg, ids)` that uses locked_rmw's `current` (`lambda cur: sorted(set(cur or []) - ids)`) and have reconcile call it with only the ids it decided to clear, instead of save_blocked(full stale set). Same primitive fixes the residual load→save windows at autopilot.py:990-992 and 1885-1886. Also move the whole reconcile pa
- **[P1] reconcile silently deletes parked decisions whenever the ticket's Jira status is not Blocked-like — but the park's 'Blocked' transition is best-effort and its failure is silently swallowed, so a decision's persistence depends on a network side effect nobody verifies**
  - scenario: A ticket escalates a question to the Commander; the Jira 'Blocked' transition fails silently (e.g. the AUTO project's workflow names the state 'On Hold - Client' or the token hiccups). Within 10 minutes reconcile classifies the still-In-Progress ticket 'active' → drops the pending decision from pending_decisions.json (commit is permanent — needs_sync.py:121) and un-parks it. The Needs-you card van
  - fix: Two guards: (1) in _park_on_tracker, detect and audit-record a failed Blocked transition (it currently returns None for both 'dry-run, intentionally skipped' and 'transition failed'); (2) in reconcile, only the DONE-like class should drop a pending decision — for 'active', keep any pending decision younger than some grace window (e.g. entry ts within 2× ttl) or whose park transition is known-faile
- **[P2] pending_decisions.json corruption is asymmetric: every reader silently maps it to 'no decisions' while every writer raises — parked decisions vanish from all surfaces with zero operator signal**
  - scenario: pending_decisions.json is corrupted once (hand-edit during an incident, partial disk restore, an errant tool). From that moment: the cockpit shows zero pending decisions (all parked questions silently invisible — the Commander believes nothing needs him), the Jira-answer resume scan sees nothing to resume (parked tickets strand forever since their blocked entries persist but their questions are go
  - fix: On JSONDecodeError in decisions.load: quarantine the corrupt file (rename to pending_decisions.json.corrupt-<ts>), notify.send a SEVERE alert, and audit.record the event — then return []. Give the writers corrupt_to_default=True so add/resolve keep working against the quarantined-then-reset store. The quarantined copy preserves the decisions for manual recovery instead of losing them.
- **[P2] blocked_tickets.json corruption fail-opens to 'nothing parked' with only a console print — the next cycle-end save persists the wipe and every parked ticket re-runs**
  - scenario: The file gets corrupted (manual edit during an incident recovery, torn copy during a state-dir restore). The drain's next cycle un-parks 6 parked tickets; each is re-picked over the following cycles, re-built at full token cost, and re-parks with duplicate Jira Blocked transitions and duplicate Telegram 'Parked (need you)' pings — Roman only finds out from the duplicate pings, and only if he corre
  - fix: In the JSONDecodeError branch: quarantine the corrupt file to blocked_tickets.json.corrupt-<ts>, audit.record('blocked_store_corrupt'), and notify.send — keeping the fail-open return set() (the code's chosen direction) but making the mass un-park loud and the original data recoverable.
- **[P2] group_chat.jsonl is the one JSONL store written with a bare unlocked append — concurrent writers can interleave torn lines that the reader then silently drops**
  - scenario: The Commander messages the group via Telegram while a cockpit /group POST is in flight (two threads in serve, or serve + a CLI council command as separate processes): two appends interleave beyond the single-write atomicity guarantee for large replies, producing a torn line. group_messages drops it silently — a Commander instruction or officer reply disappears from the thread context that later gr
  - fix: One-line fix: route _append_group through locking.locked_append(_group_file(cfg), json.dumps(row)) — the primitive exists precisely for this (locking.py:57-76). Optionally add a size-triggered trim (keep last N lines) since nothing bounds the file.

## security
Probe.

- **[P2] t**
  - scenario: f
  - fix: p

## concurrency
Audited the 1-day-old N=2 concurrent-drain seams: loop.py _run_inner_concurrent/_pick/_tickets_conflict/_ticket_footprint/_host_free_gb, the _land path (EU-379 race machinery), _worktree_lock/slot handling, and git_ops.reap_stale_worktrees, plus live state evidence. Verified-good: _land is truly synchronous end-to-end so same-process lands cannot interleave; cross-process land races are absorbed by the bounded EU-379 re-trial (loop.py:2761-2799) with the gate honestly re-run; audit appends are flock+thread-lock serialized (audit.py:35-78) and the usage ledger uses locked_append (usage.py:137); the reaper's protected set now covers every slot path across range(max_concurrent_builders) (git_ops.py:89-99), closing the EU-334 self-reap class for slot worktrees; fragment front-injection still f…

- **[P0] Sync phases on the shared event loop silently consume the sibling slot's wall-clock LLM budgets and stall its agent stream**
  - scenario: Slot 0 lands a CI-relevant ticket (up to 300s CI poll + trial gate + sentinel suite + pushes) while slot 1 is mid-review (900s cap) or deep in a long builder pass: the blocked loop consumes the sibling's remaining budget, the pass is reported as 'wall-clock timeout after Ns' (agent.py:461) — a spurious failure attributed to the WRONG ticket — triggering the turn-limit retry ladder or a FAIL verdic
  - fix: In the concurrent path, offload every long sync phase to a worker thread (await asyncio.to_thread(run_gate,...), same for base_gate_check, sentinel.guard, ci_conclusion.check, _repin_worktree_deps, and the git network calls) so the loop stays live — lands remain logically serialized with an asyncio.Lock around the land section. At minimum, stop charging the sibling: track loop-blocked time and ext
- **[P1] WorktreeBusy slot is never un-marked: after the blocker exits, the drain ACQUIRES the lock, still skips every ticket of that app, and starves other runs**
  - scenario: A manual cockpit run holds automatixy's slot-0 worktree when the drain first picks an automatixy ticket → slot_busy. The manual run finishes minutes later; the drain's next automatixy pick successfully takes the flock (now held until the drain ends) yet still reports every remaining automatixy ticket 'deferred — worktree busy (another run active)' — a false status — while simultaneously blocking a
  - fix: On successful enter_context, slot_busy.discard(key); restructure so the lock is attempted exactly once per key and its held/failed state is tracked explicitly (e.g. a dict key→'held'|'busy') instead of inferring from gits membership; release the lock immediately if the ticket is going to be skipped anyway.
- **[P2] Footprint-conflict guard trusts ticket text, but real builds demonstrably edit well beyond the cited paths — co-scheduled 'disjoint' tickets can collide on shared files**
  - scenario: Two same-app tickets citing disjoint dirs co-build; both builders touch a shared hot file (playwright.config.ts, bun.lock, en.json). Second land's trial_merge is dirty → LandRace machinery can't save it (loop.py:2778 break) → REQUEUED for a full from-scratch rebuild (AUTO-110 scale: ~$13, 90 min) or PR_OPENED needing a human — the exact double-work the guard was ordered to prevent.
  - fix: Treat known shared hot files/dirs (lockfiles, playwright/vitest configs, translation catalogs, CI workflows) as always-conflicting within an app; re-check the conflict after the planner emits in_scope_files and defer the second ticket then (before builder spend) if the real scopes overlap.
- **[P2] Memory floor is a single 30s-poll sample taken only BEFORE pick — slot 1 commits to a multi-hour build on one good sample, recreating the 2026-07-17 dual-OOM window**
  - scenario: Host hovers near 6GB free; one sample reads 6.2GB → slot 1 picks an XL ticket whose playwright/bun subprocesses spike memory 10 minutes in, alongside slot 0's build → macOS kills both builder subprocesses (exit-143 x2), losing two in-flight builds — exactly the incident the floor exists to prevent, because the floor cannot see pressure that arrives after commit.
  - fix: Require N consecutive samples above floor+hysteresis (e.g. 2 samples ≥ floor+2GB) before re-admitting slot 1, and add a mid-ticket pressure check at pass boundaries (planner→builder→reviewer seams) that pauses slot >0 before launching the next subprocess-heavy pass.
- **[P2] _pick demotes every deferred ticket to the queue tail, so a top-ranked conflicting ticket lands LAST**
  - scenario: A 15-ticket drain where the Commander's top-ranked ticket conflicts with the first in-flight build: it is re-appended behind 13 lower-priority tickets and, since each new in-flight neighbor may re-defer it, the highest-priority work ships hours later than rank ordered — the drain silently inverts Jira Rank.
  - fix: Scan the deque without net reordering: iterate by index (or rotate the deque back to its original orientation after the scan) so deferred tickets keep their position and are re-tested first when the conflict clears.
- **[P2] A base-halt from one slot never reaches the sibling's in-flight build of the same app**
  - scenario: Slot 0 starts an automatixy ticket, base_gate_check comes back red → base_halt_run; slot 1 is 10 minutes into a 45-minute, ~$10 build of another automatixy ticket on the same red base. The build completes, trial-merge gate fails against the red base, ticket goes PR_OPENED/requeue — the whole build spend was predictably wasted the moment the halt fired.
  - fix: On base_halt_run, set a per-app halt flag that process_ticket's existing stop checkpoints (pre-build/pre-merge, the stop_event seams in _attempt) consult, so a same-app sibling aborts at its next pass boundary instead of running to a doomed land. Cheap alternative: at minimum skip the reviewer+land passes for in-flight tickets of a halted app.
- **[P2] Shared dollar budget lets N slots overshoot max_cost_usd by N tickets instead of one**
  - scenario: Budget nearly exhausted at a drain's end: both slots pick XL tickets simultaneously → the cap is exceeded by two full ticket costs (~$25+ observed scale) before either boundary check fires, with budget_stop recorded only after the fact (loop.py:1087-1088).
  - fix: Reserve an estimated per-ticket cost at pick time (pre_flight_check already estimates 8%/ticket — usage.py:530) and refuse the pick when remaining < in-flight reservations + estimate, so concurrency cannot multiply the overshoot.

## failure-paths
Failure-path audit of the terminal-outcome surface (contracts.Outcome MERGED/PR_OPENED/ESCALATED/ERRORED/SKIPPED/REQUEUED) across loop.py, autopilot.py, decisions.py, needs_sync.py, scrum.py, sentinel.py, notify.py, audit.py, with live state/ evidence. The land path, sentinel revert, red-base, turn-limit and no-changes paths are well-hardened (guarded set_status, reconcile audit events). The critical gap is the ESCALATED park pipeline: decisions.add silently voids PM-authored escalation questions containing markdown headers (returns None, no Jira Blocked, no pending decision), and the new needs_sync reconciler then un-parks any ticket still In Progress — together producing a live, token-burning escalate→unpark→rebuild loop (AUTO-182, 2026-07-20: 4.5M-token attempt, needs_human question sta…

- **[P0] decisions.add silently drops PM-authored escalation questions — park voided, board never Blocked, Telegram reply-hint dead, ticket rebuild-churns**
  - scenario: Any escalation whose question text carries a markdown header or a 'Based on the…' opener (i.e., most PM-triage and reviewer needs_human texts): loop reports ESCALATED and Telegram says 'needs you' with a reply hint — but no pending decision exists (a 'AUTO-182: <answer>' reply resolves nothing), Jira is never moved to Blocked, the cockpit Needs-you stays empty. needs_sync (autopilot.py:1873, every
  - fix: Never drop a park: on validation failure, fall back to a sanitized question (strip '#' headers / wrap the raw text under a one-line generic ask) instead of returning None; keep the None return only for truly empty text. Independently, make every caller handle None (at minimum: still set_status('Blocked') + post the raw text as a comment + audit a 'park_question_rejected' event). Pin with a test th
- **[P1] needs_sync un-parks any ticket still active in Jira, but PR_OPENED and run-budget ESCALATED parks never transition Jira — park↔unpark rebuild oscillation**
  - scenario: A ticket passes review but merges dirty (concurrent drain land race or dev-gate red after merge — 3 historical pr_opened events, all 'dev gate fails after merge'): PR opened, ticket parked in memory only, Jira stays In Progress. Within one needs_sync TTL (600s) the park is cleared and the PR card dismissed; next cycle the drain rebuilds the entire already-reviewed ticket on a fresh branch (~6M tok
  - fix: Make every park Jira-visible: in the PR_OPENED branch call backlog.set_status(ticket, 'Blocked') (guarded) or decisions.add with the PR URL as the question; give the run-budget ESCALATED site the same decisions.add + notify plumbing as the per-ticket budget site (loop.py:1754-1766). In needs_sync, only clear a parked entry on 'active' when the park is confirmed Jira-backed (e.g., the pending entry
- **[P1] Usage/plan-limit refusal storm still Blocked-parks the head-of-queue ticket: strike threshold equals barren-cycle threshold and plan_limit_hit's 'blind' flag is consumed nowhere**
  - scenario: The 5-hour Max window exhausts mid-drain (a routine biweekly event per memory): probe goes blind → hit=False → drain keeps dispatching; the top-Rank ticket is re-picked in each of the 3 barren cycles, instant-fails 3× on refusals, and is moved to Blocked in Jira with a 'consecutive errors' comment — a healthy ticket parked for a provider outage, requiring a manual /unblock (or a needs_sync round-t
  - fix: Consume blind: in the autopilot plan-limit block, treat plan_check['blind'] (or N consecutive blind checks) as a hold — do not dispatch on an unverifiable window. Additionally add the SDK refusal signature (the agent.py 'transient' refusal text) to auth_probe/infra_classify as a no-strike class, or arm the usage breaker at _MAX_TICKET_ERRORS-1 barren cycles so the hold always fires before any tick
- **[P2] Three unguarded Jira writes on otherwise-decided paths convert ESCALATED/MERGED outcomes into ticket_exception (the exact EU-367 class, three sites missed)**
  - scenario: Jira returns a 4xx/timeout on exactly one of these comments: (a) smoke path — an already-pushed merge is reported ERRORED 'ticket_exception'; the MERGED report never returns, so recently_merged (autopilot.py:1617) is never set, an error strike is charged, and the changelog/audit disagree with the TicketReport; (b) needs_human paths — the park decision was recorded but the run reports ERRORED, the 
  - fix: Wrap each of the three add_comment calls in the same try/except-audit pattern as their guarded siblings (record tracker_reconcile_needed with phase and continue to the intended return). Add a guard-style test asserting no bare backlog.add_comment/set_status call sites exist between an outcome's first side effect and its return (mirroring the 2026-07-19 'only unguarded set_status' audit).
- **[P2] scrum.split parent-close failure is completely silent — parent stays open with ok=True, next drain re-splits and files a duplicate Epic + children**
  - scenario: A transient Jira error (or a workflow with no 'Done' transition from In Progress) on the parent close: fragments are filed and built, but the oversized parent remains To Do/In Progress. Next drain re-picks the parent, blows the same turn/budget ceiling, splits again → a second Epic with a duplicate child set (the memory note 'avoid dup tickets' documents this exact board pathology), doubling built
  - fix: On set_status failure, set result['error'] (or a 'parent_open' flag), record an audit event ('split_parent_close_failed') and include the fact in the loop's Telegram notify; before splitting, check for an existing 'Auto-split from <id>' child or the parent's own split comment and refuse a duplicate split, escalating instead.
- **[P2] Manual-test land: when the post-merge 'Needs Human' transition fails, Telegram still announces 'moved to Blocked — exact manual test steps are on the ticket'**
  - scenario: Jira hiccups on the transition after a manual-test-required merge (the task's exact 'what if set_status Needs Human throws AFTER merge' probe): the code is live on dev, the Commander's phone says the steps are on a Blocked ticket, but the ticket is In Progress with no steps anywhere; the manual verification silently never happens and the only trace is one audit event nobody is paged on — an unveri
  - fix: Capture the transition outcome in a local flag inside the except; when it failed, change the notify tail to the reconcile instruction (mirroring the sentinel-revert message at loop.py:2943-2945: 'tracker update FAILED — set it to Needs Human by hand, steps follow:' + the manual block inline in the Telegram message), and add a decisions.add(block=False) fallback so the cockpit Needs-you carries the
- **[P2] AuditLog.record is not exception-safe (callers explicitly rely on the opposite): disk-full/read-only state/ turns lands into ticket_exceptions and can kill the drain inside its own error handler**
  - scenario: The disk holding state/ fills (audit.jsonl+logs grow ~7MB/month, plus run logs/transcripts) during an unattended overnight drain: the first audit.record after a successful push raises OSError → _exception_report's own record raises inside the except → the exception escapes the per-ticket guard, the drain dies mid-cycle (blocked-set/error-count/recently_merged bookkeeping for the cycle's reports ne
  - fix: Make record() swallow OSError (throttled stderr note mirroring notify._note_send_failure) so the audit trail degrades instead of the pipeline; pass return_exceptions=True to the gather in _run_inner_concurrent (log the exception, keep the other slot's reports); add a health.py 'bad' check for free disk on the state/ volume so the condition pages before it bites.

## resources-cost
Resource & cost protection audit of the General orchestrator (dev @ 3daf3ea). The defenses that exist are real: daily token governor (100M, autopilot.py:1677), per-ticket 3M/90min budget (loop.py:1728-1742), turn-limit requeue is genuinely fail-closed and bounded to one retry (builder.py:257-271, loop.py:467-476), scrum split depth-capped at 3 (scrum.py:68,124-128), error-strike parking capped at 3 (autopilot.py:541), log purge verified live (log_retention_days=30 in config.yaml:143, run_logger.py:72-74, state/logs=2.1M), usage-ledger prune runs flocked at CLI start (usage.py:690-705, main.py:439), worktree reaper has self/canonical exclusion and only one 518M canonical worktree exists. The holes cluster around the two newest features: HYBRID mode (2026-07-19) routes every build to GLM whi…

- **[P0] Hybrid mode routes all builds to GLM but every GLM budget gate keys off the MAIN backend — GLM dispatch is ungoverned in the live config**
  - scenario: z.ai GLM quota exhausts mid-drain (glm_budget_status shows over/bad but nothing reads it on this path) → every builder pass instant-fails with a cap-classified GLM error → plan_limit_hit sees healthy Claude, hit=False → the drain keeps picking; healthy tickets accrue error strikes and get parked Blocked; the only brake is the 3-barren-cycle breaker's 30-min cooldown loop, churning and false-parkin
  - fix: In _glm_budget_preflight_block (loop.py:797), also block (or disarm hybrid for the run, falling back to main-model builds) when backend_pref.get_hybrid(cfg) is true, the secondary normalizes to GLM, and glm_budget_status is over/bad. Symmetrically, make pre_flight_check/graceful_stop_check evaluate the provider that will actually execute the BUILDER (main in backup mode, secondary in hybrid), not 
- **[P1] plan_limit_hit still fails OPEN when the probe is blind — the EU-357 `blind` flag has zero consumers and pre-flight's Claude plan-limit branch is dead code**
  - scenario: The 5h Max window exhausts; the throwaway `claude -p` probe itself gets refused → plan_usage available=False → blind=True but hit=False → the plan-limit governor and the secondary-model fallback (autopilot.py:1704-1719) never engage; the drain churns 3 full worklist cycles (Jira transitions, comments, strikes) before the breaker pauses, then repeats every 30-min cooldown until the window rolls.
  - fix: Consume blind: when plan_check['blind'] is true AND the previous cycle produced cap/refusal-classified failures, treat it as hit (engage the secondary fallback or the usage hold immediately instead of waiting out 3 barren cycles). Separately, merge plan_usage()'s limits into dual_provider_budget_status's claude entry so the existing pre_flight utilization check (usage.py:559-569) actually executes
- **[P1] GLM builder passes have no in-pass token ceiling — a single pass burned 13.28M input tokens (4.4x the entire per-ticket budget) before any check could fire**
  - scenario: Any GLM-routed build in a repo with a large test suite grinds to the turn ceiling reading/re-sending suite stdout; each such pass burns 9-13M tokens; at the 100M/day GLM ceiling, ~7 runaway passes exhaust the whole day's GLM budget on unfinished work, and the 3M per-ticket budget only refuses the NEXT pass after the damage is done.
  - fix: Give GLM passes a compensating in-pass bound: agent.py already counts per-message tokens on the stream — abort the SDK stream (same seam as the EU-221 wall-clock timeout, agent.py:363-373) once cumulative pass tokens exceed ~2x budget_for(); or reduce max_turns for GLM-routed builder calls so the turn cap approximates the missing task budget.
- **[P2] Usage-exhaustion storms park healthy tickets as Blocked: _MAX_TICKET_ERRORS (3) == _MAX_BARREN_CYCLES (3), so the top ticket is falsely parked on the exact cycle the breaker fires**
  - scenario: A 2h remainder of an exhausted 5h window falsely parks ~3-4 perfectly healthy, highest-priority tickets as Blocked with 'need you' Telegram pings; each requires a manual /unblock, and the board lies (Blocked ≠ anything wrong with the ticket).
  - fix: Classify plan-limit/cap refusal text as no-strike, same as auth_probe login failures in _tally_errored (autopilot.py:1239) — add a usage_probe.is_cap_refusal(notes) split; or refund the strikes charged during the barren window when the usage hold arms.
- **[P2] Budget and plan-limit governors run only at cycle top, but a cycle is unbounded — fragment injection extends the live worklist for hours with no re-check**
  - scenario: The daily 100M ceiling is crossed one ticket into a fragment-chain cycle; the drain keeps building every remaining queue item for hours — with two builders and 3M/ticket, a multi-ticket chain overshoots the ceiling by tens of millions of tokens; a mid-cycle 5h-window exhaustion likewise churns the whole remaining worklist before the breaker can observe even one cycle boundary.
  - fix: Re-check usage.budget_status / plan_limit_hit at the ticket boundary inside _run_inner_serial/_run_inner_concurrent (the same seam that honours stop_between_tickets) and stop picking when over — mirroring the graceful-stop 'finish the in-flight ticket, start nothing new' contract.
- **[P2] Memory floor is pick-time-only — nothing re-checks while two builds run, so simultaneous gate/e2e spawn bursts can still OOM both slots (the 2026-07-17 incident shape)**
  - scenario: Both slots pick at ~8GB free (floor passes), both reach their gate phase together: two vitest worker pools + chromium instances push free memory below jetsam thresholds → macOS kills both builder processes at once → two in-flight builds and their worktree state lost, drain reports two spurious ERRORED strikes.
  - fix: Re-check the floor at the phase boundary before running the gate (the memory-spiky phase) and before each retry pass, standing the slot down until pressure clears; or serialize gate execution across slots with an asyncio.Semaphore(1) so at most one suite runs at a time.
- **[P2] scrum.split: a failed parent-close after a successful split still returns ok=True — the open parent is re-picked, re-built, and re-split into duplicate epics and children**
  - scenario: One Jira transition failure (network blip, workflow missing the Done transition) at parent-close → next cycle re-picks the still-open oversized parent → burns another full oversized attempt (up to 3M tokens / 90 min) → splits again → duplicate Epic and duplicate children on the board, every one of which the drain will subsequently build — doubled cost and duplicate landings.
  - fix: Treat a failed parent-close like partial filing: verify the transition landed (re-read status) and set ok=False + the INCOMPLETE comment when it didn't; additionally track split parents in a recently_split cooldown set beside recently_merged in the autopilot pick filter.

## ops-deploy
Deploy & operations audit of /Users/romanberlin/Projects/General. The deploy story has one armed P0: the 2026-07-20 case-insensitive changelog fix (6daac70) made cross-repo lands commit to the General checkout's dev but push to the landed app's branch name ("DEV"), which fails — the checkout is 1 commit ahead of origin/dev right now, and the next Elite-Unit land is guaranteed to diverge local dev from origin/dev, silently re-arming the EU-335 stale-cockpit class AND making the EU-387 self-restart refuse (dirty tree) so the unit keeps running old code while announcing it deployed. Beyond that: nothing mechanical stops a deploy from killing a live build (serve has no SIGTERM handler; active_run_count is consulted only by the EU-387 idle exit; the VPS self-update restarts unconditionally on a…

- **[P0] Changelog commit pushes the WRONG branch name on cross-repo lands — local dev is already 1 commit ahead and the next Elite-Unit land guarantees a silent stale-cockpit divergence (EU-335 class re-armed)**
  - scenario: Next Elite-Unit ticket lands (or Roman kickstarts after one): origin/dev moves without 3daf3ea → the primary checkout can never fast-forward again → cockpit runs stale code indefinitely with zero alert (the exact '0 shipped all night' EU-335 incident), self-update auto-restart permanently refused, and each automatixy land stacks another unpushed commit deepening the divergence.
  - fix: In loop.py:190 stop passing the landed app's base: the changelog repo is always the General checkout, so pass the General repo's own branch (hardcode 'dev' or derive from the Elite-Unit app config), and have _git_commit_changelog push the branch git actually reports (the `branch` var from line 113) rather than the caller's `base`. Immediate remediation: `git push origin dev` from the primary check
- **[P1] Nothing mechanical stops a deploy/restart from killing a live build — kickstart safety is a memory-only human rule, serve has no SIGTERM handler, and the VPS self-update restarts unconditionally**
  - scenario: Roman (or any runbook/agent) kickstarts the cockpit to pick up landed code while a builder is mid-ticket: the SDK subprocess dies mid-edit, the worktree is orphaned with WIP, the review/land never happens, and EU-385 re-picks the ticket from scratch on boot — burned tokens plus a half-built branch; on the VPS the same happens automatically every time main is promoted during a run.
  - fix: 1) Install a SIGTERM handler in server.serve that sets every active run's stop_event and waits a bounded grace period before exit. 2) Expose the existing EU-387 machinery as an operator command — `./general restart-when-idle` / POST /api/restart — that writes the self_restart_pending flag (autopilot.flag_self_update) instead of documenting kill discipline. 3) Make scripts/self-update.sh query the 
- **[P1] A bad land on the unit's own repo respawns INTO the bad code with no Mac smoke test, no start limit, no boot alert, and no documented rollback — launchd crash-loops it forever**
  - scenario: The unit lands an EU ticket that passes the worktree gate but fails at serve boot (import cycle, config-schema change, missing new dep in the main .venv): the drain goes idle, exits 75, launchd respawns into a 5-second crash loop at 3am — cockpit down, Telegram silent, drains dead, and EU-385's RUNNING intent can never resume; discovery waits until Roman opens the cockpit.
  - fix: Mirror EU-184 on the Mac: before os._exit(75) in _maybe_self_restart, run the same import smoke-test against the landed checkout (`.venv/bin/python -c 'import orchestrator.main, orchestrator.server, ...'`) and refuse the restart + alert on failure. Add a crash-loop tripwire at serve boot (a boot-count file: N boots within M minutes → Telegram alert via a bare curl that needs no orchestrator import
- **[P1] No out-of-process watchdog: a wedged-but-alive serve process is invisible at 3am — the only heartbeat is a cockpit UI chip, and a dead Telegram degrades to a console line nobody reads**
  - scenario: The drain wedges at 01:00 on a hung subprocess or an SDK call outside the EU-221/EU-366 timeout seams: process alive, launchd satisfied, no cycle boundary reached, no notify fires. Roman finds out at 09:00 when he opens the cockpit — eight hours of the exact 'drain wedged and nobody knew' outcome the system is supposed to preclude.
  - fix: Add a tiny external watchdog (launchd StartInterval agent or the existing autopull script): curl the cockpit for a machine-readable heartbeat (last_activity age + active_run_count + drain intent) and send Telegram directly via curl if the drain intent says RUNNING but activity is older than N minutes, or if the HTTP probe itself fails. Being out-of-process, it also covers full-process hangs and do
- **[P2] .env and config.yaml are unversioned single points of loss with no backup mechanism, and config.example.yaml has drifted from the live schema**
  - scenario: Disk failure, an errant cleanup, or a bad agent edit destroys .env (Jira/Telegram/GLM tokens) or config.yaml: the unit is down until every credential is re-issued and the 153-line config is reconstructed from memory — the .bak restores a month-old shape that current code's schema may reject; meanwhile the Mac and VPS silently run different app/gate configurations.
  - fix: Add an encrypted or private backup path for the two files (e.g. the existing state-sync git clone already used by sync.py, or a nightly copy into Documentation-excluded private storage), refresh config.yaml.bak automatically on every config edit via the cockpit onboard form, and add a guard test that diffs config.example.yaml's key set against orchestrator/config.py's accepted keys so the example 
- **[P2] The installed cockpit launchd plist is the STALE pre-stabilization one — it still bakes ~18 ephemeral Claude-session dirs into PATH and lacks ~/.bun/bin; the 2026-07-19 installer fix was never deployed**
  - scenario: A gate step resolves a binary through one of the ephemeral session dirs (they shadow later PATH entries) or needs a bun-installed global from ~/.bun/bin that the live PATH lacks: builds fail with 'No such file or directory' exactly like the 2026-07-10 bun incident, but only under the daemon — unreproducible from a terminal, costing hours of confusion; the stale session paths also silently vanish w
  - fix: Re-run `bash scripts/install-mac-cockpit-daemon.sh` at the next idle window (it bootouts and re-bootstraps with the curated PATH) — respecting the no-kickstart-during-run rule since reinstall restarts the cockpit. Longer term: have the installer (or a health.py check) compare the installed plist's PATH against the script's template and flag drift in `general doctor`.
- **[P2] Mac/VPS operational state is per-host with no reconciliation — parked tickets, model prefs, and drain intent do not propagate, and the VPS runs code 20 commits behind**
  - scenario: A ticket parked on the Mac after 3 errors (or an explicit /unblock park) is invisible to the VPS: if the VPS's serve ever drains the same project (both hosts run identical autopilot code and the same Jira creds), it re-picks the parked ticket and burns the 5h window re-failing it; conversely a Commander stop honored on one host does not exist on the other. Model-pref divergence means the same tick
  - fix: Decide and enforce single-drainer topology mechanically: either give the VPS a config flag that disables autopilot for shared projects (asserted by health.py), or fold blocked_tickets/model prefs into the existing sync channel (they are small JSON — publish them in the host's shared jsonl and merge on pull). Update DEPLOYMENT.md's stale paths while touching it, keeping the eu43 docs-reality guard 

## new-features
Adversarial review of the 2026-07-19/20 feature code (needs_sync, squad_pref, review-verdict reconciliation, exec-gate escalate-once, manual-test landing, turn_retries, changelog case fix, EU-231 self-app routing). Two P1s: (1) needs_sync.reconcile writes the parked-ticket set back from a snapshot taken BEFORE its minutes-long serial Jira scan — the exact stale-write hazard the neighbouring code in autopilot.py explicitly documents and defends against (EU-218 'came back from the dead twice'), so parks/unparks made during the scan are silently lost; (2) the FAIL→PASS reconciliation block tests only quality_issues severity — required_changes, spec_gaps, and non-canonical severity strings ('critical', 'high') are all invisible to it, so a reviewer FAIL carrying its findings in those channels …

- **[P1] needs_sync.reconcile writes the parked set from a pre-scan snapshot — parks/unparks made during its minutes-long Jira scan are silently lost**
  - scenario: N=2 builders are draining. reconcile starts (drain cycle or /needs load), snapshots parked={A,B}, and begins a 40-ticket Jira scan (~1-20 min against a slow/half-open Jira). At T+30s a build hits the EU-219 consecutive-error park (autopilot.py:2123-2128) and adds ticket C; or the Commander /unblock's A via Telegram. reconcile finishes and writes its stale snapshot: C is silently un-parked (the nex
  - fix: Never write a snapshot: compute the set of ids to UNPARK during the scan, then apply it inside one locked read-modify-write — `locking.locked_rmw(_blocked_file(cfg), lambda cur: sorted(set(cur or []) - to_unpark), default=[])` — so concurrent adds/removes survive. Also hold a body-level guard (e.g. a second lock or a 'running' flag) so force=True can't run concurrently with an in-flight pass, and 
- **[P1] FAIL→PASS reconciliation checks only quality_issues severity — required_changes, spec_gaps, and non-canonical severities ('critical') are invisible, so a real reviewer FAIL ships with its demanded fixes silently dropped**
  - scenario: A reviewer (especially a GLM-5.2 hybrid-build fallback) returns {verdict:'FAIL', spec_met:true, quality_issues:[{severity:'critical', detail:'SQL injection in the new filter param'}], required_changes:['parameterize the query']}. 'critical' matches neither blocker nor major → blockers=[] and blocking_issues=[] → rule (a) fires on pass 1 → verdict rewritten PASS → is_ship_ready() true → the change 
  - fix: Three small tightenings: (1) in _parse, normalize severity — map anything outside {'blocker','major','minor'} (critical/high/sev1/…) to 'blocker', or at minimum treat unknown severities as blocking; (2) add `and not review.required_changes and not review.spec_gaps` to the rule-(a) condition (a FAIL that names concrete required changes is not 'inconsistent' — it is the reviewer doing its job); (3) 
- **[P2] needs_sync._classify treats every unrecognized Jira status as 'active' — a renamed/custom blocked-ish column silently drops pending decisions and un-parks genuinely waiting tickets**
  - scenario: A board uses 'On Hold - Waiting for Customer', 'In Review', or 'Waiting for approval' (none exact-match the 4-entry set) for a ticket parked on a Commander decision. The next reconcile pass — which runs automatically every /needs page load and every drain cycle — classifies it 'active', permanently deletes the pending decision entry (the question the Commander never answered), un-parks the ticket,
  - fix: Invert the fail-open default: add an explicit _ACTIVE_LIKE allowlist ('to do', 'in progress', 'backlog', 'open', 'selected for development', …) and return 'unknown' (→ kept, needs_sync.py:110-112 already keeps unknown) for anything not in one of the three known sets, plus a substring match for the blocked family ('on hold', 'waiting', 'blocked' as prefixes/contains). Destructive clearing should re
- **[P2] turn_retries.json entries are never cleared on success — a ticket that once blew the turn ceiling is permanently effort-boosted and has permanently spent its one-shot requeue**
  - scenario: EU-450 blows the turn ceiling once in July, gets its boosted requeue, and lands. In September the Commander reopens EU-450 for a small follow-up: the build now runs one effort level higher (turns_for scales 1.6-2.4x → materially higher token cost per builder.py:329-335) with no signal beyond a reason string. Worse: if that later run genuinely blows the ceiling again, `turn_retry_count == 0` is fal
  - fix: Clear the marker when the ticket resolves: on Outcome.MERGED (and on the ESCALATED park, where the counter's job is done) pop the ticket id inside a locked_rmw — e.g. add builder.clear_turn_retry(cfg, ticket_id) and call it from loop._resolve / _land. If the boost is intended to persist across the single requeue only, this also restores the documented 'once' semantics for any future life of the sa
- **[P2] _manual_test_block regex is unanchored with no negative-answer handling — 'MANUAL TEST: none required' or a quoted contract line routes a fully verified land into Blocked; multiline steps truncate at any CAPS heading**
  - scenario: Trigger 1 (likely): a compliant-but-chatty Builder writes 'MANUAL TEST: none — all ACs verified by the test suite' in its summary; block='none — all ACs…' is truthy → a machine-verified land is moved to Blocked with the hand-off comment 'needs YOUR manual test / MANUAL TEST STEPS: none — …', stalling Done work in the Blocked column and paging the Commander for nothing. Trigger 2: an EU ticket touc
  - fix: Anchor and sanitize: match `^MANUAL TEST:` with re.M so only a section heading counts; after capture, treat `^(none|n/a|not (needed|required)|nothing)\b` (case-insensitive) as no block; prefer build.summary and only fall back to raw when summary is empty; and loosen the terminator to a known-heading set (`\nTEST:|\nIMPROVEMENTS:|\Z`) instead of any CAPS word so numbered steps with blank lines surv
- **[P2] Changelog branch guard compares the orchestrator checkout's branch against the PRODUCT app's base_branch — the case-insensitive fix papered over a wrong-variable bug that resurfaces for any app whose base isn't 'dev'**
  - scenario: Onboard any app with base_branch 'main' (or 'develop'). Every successful land of that app appends its changelog line (locked_text_rmw succeeds) but the commit is skipped ('on dev, not base main') → Documentation/Development_Status.md stays permanently dirty in the primary checkout → ~/bin/general-autopull.sh's clean-tree fast-forward guard never passes → the cockpit silently serves stale code all 
  - fix: Decouple the changelog repo's branch from the landing app: the target repo is always the orchestrator, so pass the ORCHESTRATOR's own base — hardcode base="dev" at the loop.py:190 call site (or resolve it from the Elite-Unit app entry / `git symbolic-ref refs/remotes/origin/HEAD` in cwd), keeping the case-insensitive compare as a secondary hardening.
- **[P2] Exec-gate escalate-once: pass-2 ship is NOT silent on default config, but mark_done_on_merge=True silently disables the entire manual-test hand-off for never-satisfiable ACs**
  - scenario: Commander flips mark_done_on_merge=True during a high-throughput drain week. A ticket carries 'AC: passes on Mobile Safari' which no repo gate runs: pass 1 exec-gate FAILs it, pass 2 demotes it, the LLM verdict ships — and the ticket is marked Done with zero human signal that the Safari check never happened anywhere: not in Jira status, not in a comment, not in the audit trail (manual_test_require
  - fix: Decouple the two knobs: compute `manual = _manual_test_block(build, review)` unconditionally; when mark_done_on_merge is set AND manual is non-empty, still post the MANUAL TEST STEPS comment and record manual_test_required (optionally still transitioning to Done), so the human signal survives the convenience flag. Optionally persist the bounced fingerprint set next to turn_retries.json so a requeu
- **[P2] _self_app matches on exact resolved-path equality between the running checkout and config repo_path — any VPS/alternate-checkout deployment silently reverts infra tickets to the victim product backlog**
  - scenario: On the VPS (or after moving the Mac checkout), the deployed config registers the unit at /root/General while the daemon actually runs from /opt/general (or the config simply omits an Elite-Unit app entry because the VPS only drains product repos — both checkouts are valid git repos, so config validation at config.py:131-133 passes). _self_app returns None on every sweep with zero warning, and the 
  - fix: Make the match robust and the degradation loud: prefer an explicit marker (an `is_self: true` flag on the app entry, or match by backlog project_key == 'EU') over path identity; keep the path check as a fallback with samefile() semantics; and when apps are configured but none matches self, emit one audit/warning line ('infra tickets will file into product backlogs — unit not registered as an app')
